---
scope: Update legacy architecture.md: bind VAL-C01..C05 + VAL-H01 as stable AD-n entries with legacy-label map
purpose: Clear the 5 critical findings from the 2026-09-05 validation gate; unblock Story 4.9
altitude: system (Works domain module + platform-host boundary)
updated: 2026-09-06T16:42
---

- (decision) VAL-C01/AD: platform host = Hexalith.Platform (github.com/Hexalith/Hexalith.Platform), owner = Platform Maintainer (Hexalith), single Aspire 13.5.x family (platform upgrades from 13.4.6 when Works composition lands). Binds Story 4.9 destination + conformance lane (verify-works-host + 4.9 acceptance scenarios run from platform). Prevents: Works and platform each assuming the other owns reminders/cascade/checkpoints/subscriptions/projections; mixed unproven Aspire graph.
- (constraint) Seam allocation default (approved proposal): missing reusable runtime capability lands in Hexalith.EventStore SDK first, then consumed by Works; domain-specific handlers stay Works-owned behind SDK interfaces; topology/composition is Hexalith.Platform-owned.
- (decision) VAL-C02/AD: per-tenant event-sourced Work-Tree Registry aggregate is the atomic authority for child->parent edges. Concurrency boundary = registry single-writer actor + ETag save. Attach = reserve edge in registry (EdgeReserved / domain rejection), reactor mechanically drives SpawnChild; SpawnChild caller facts demoted to assertions checked against registry-derived ancestry/depth. Registry authoritative over item streams on disagreement; rebuild suppresses dependent roll-up/cascade for divergent subtrees until repaired. Prevents: two parents attaching one child; cycles from stale caller ancestry.
- (decision) VAL-C03/AD: FR-11 live recursive roll-up = registry-backed fan-out. Ordinary child-event projection delivery resolves ancestor chain from tree-registry read model and updates each ancestor's per-(childId, childEventSequence) LWW contribution slot; unresolvable ancestry => delivery not acked (retry), no partial ack. Missing generic relationship-lookup seam lands in EventStore SDK first. FR-11 unchanged; 'unavailable' semantics reserved for genuine repair/rebuild windows. Prevents: Works projection with no graph input vs platform persisting null rolled values.
- (decision) VAL-C04/AD: identity provenance = platform-edge OIDC at Hexalith.Platform ingress; authoritative tenant/actor derived from verified claims via Hexalith.Tenants membership; envelope/payload tenant+actor are assertions only, mismatch denied before dispatch with no tenant-existence disclosure; internal originators (reactor/reminder/replay/recovery) use workload identity + audited tenant delegation. Authz floor: commands=tenant member; queries=member+result filtering; admin/rebuild/replay/quarantine=platform-operator role, audited. Enforcement + negative tests = Story 4.9 platform acceptance. AuthorityLevel stays carried-not-enforced.
- (decision) VAL-C05/AD: trusted origin fully bound: Dapr mTLS with declared trust domain/namespace; deny-by-default route + network policy (no direct workload HTTP); TLS + producer/consumer broker ACLs; exclusive originators (only EventStore publishes work.events; reminder callbacks only from Works actor identity; replay/rebuild only from authorized operations identity); negative migration tests (direct route, spoofed app id, wrong trust domain, unauthorized publish, forged high SequenceNumber) in 4.9 lane. Sandbox relaxation only via explicitly named dev-only exemption.
- (decision) VAL-H01/AD: FR-10 automatic expiry = durable per-item expiry reminders in the existing reminder subsystem (registered/cancelled/rescheduled mechanically from lifecycle events; deterministic tenant-inclusive identity; fire => ExpireWorkItem, idempotent vs terminal state; recovery via indexed reconciliation protocol). Policy authority for Due-Date/TTL defaults = Hexalith.Platform host configuration (typed per-tenant/work-type options consumed by the adapter; kernel never reads config). FR-10 unchanged - no correct-course needed.
- (direction) Also applying gate-designated autofixes: VAL-H02 dependency graph, VAL-H05 version authority (global.json + Hexalith.Builds props; Dapr runtime vs SDK split), VAL-M02 pub/sub ordering wording, VAL-M03 actor turn-lock vs ETag fallback, VAL-M04 RFC 9457 as acceptance contract. Remaining highs (H03 creation order -> correct-course; H06-H12, M01) recorded in an open-findings register with revisit conditions.
- (event) AD register (AD-01..AD-25) with Binds/Prevents/Rule, legacy-label map, AD-20 migration matrix R1-R10, and open-findings register written into architecture.md; ~35 body-prose reconciliation edits applied (platform-host naming, dependency graph, version authority, turn-lock/ETag, pub/sub ordering, ProblemDetails, key-derivation retirement, readiness/checklist updates); frontmatter updatedAt=2026-09-06
- (event) Deterministic lint (lint_spine.py via ARCHITECTURE-SPINE.md symlink): 0 AD-shape findings (no duplicate IDs, no missing Binds/Prevents/Rule); 10 low placeholder hits = known {tenant}/{domain}/{id} key-notation false positives, dispositioned ignore (same class as prior gate)
- (event) Reviewer gate: rubric walker PASS w/ concerns (7/8 checklist, was 0/8); reality lens PASS (AD-20 matches Hexalith.Platform checkout; Aspire 13.5.x/Dapr/xUnit claims web-verified); adversarial lens CONCERNS (2 critical + 5 high inside new AD-21/AD-22 protocol). Reviews under /home/administrator/projects/hexalith/works/_bmad-output/planning-artifacts/architecture/architecture-works-2026-09-06/reviews/.
- (decision) Gate fixes applied (finalize mode): ADV-U1 AD-06+AD-22 rebound to flattened per-descendant own-contribution LWW slots (single sequence space; rolled = own + sum slots; RR-1 property over >=3-level trees). ADV-U2 AD-21 saga lifecycle: Reserved->Attached/Released, additive spawn-rejection event carrying ChildWorkItemId, duplicate SpawnChild for Attached pair = no-op, cascade/fan-out enumerate Attached only, disagreement rule scoped to existing items (ghost edge auto-Released). ADV-U3+U6 AD-24: SpawnChild origin-restricted to reactor identity (public entry = registry reserve command); reminder register/cancel restricted to Works workload identity; registrar added to AD-23 delegation; positive production-policy registration test added. ADV-U4 AD-22 freshness witness: attachment registry-sequence provenance vs registry read-model watermark; roots resolve immediately. ADV-U5 reservation event carries full SpawnChild payload verbatim. ADV-U7 matrix: R7 widened to child-completion resume recovery; new R11 = IWorkCommandSubmitter seam (VAL-H10 lands there, revisit advanced to before registry story).
- (event) Post-fix lint clean (0 AD-shape findings; 10 known placeholder false positives). Update run complete: VAL-C01..C05 + VAL-H01 closed as AD-20..AD-25; autofixes H02/H05/M02/M03/M04 applied; open register carries H03/H06-H12/M01.
