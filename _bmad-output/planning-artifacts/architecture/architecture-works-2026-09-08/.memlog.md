---
scope: Update architecture.md AD register: roll in the 2026-09-08 validation gate (VAL2-C01..C03, H01..H13, M01..M13, lows) and absorb the 2026-09-08 PRD amendment (addendum handoff H1/H2/H8/H12)
purpose: Make AD-06/21/22/25 a safe substrate for the registry, fan-out, and expiry stories; re-align Story 4.9 acceptance; keep AD IDs stable
altitude: system (Works domain module + EventStore SDK seams + Hexalith.Platform host boundary)
updated: 2026-09-12T15:53
---

- (event) 2026-09-08 Update run resumed from architecture/architecture-works-2026-09-06/.memlog.md (AD-01..AD-25 bound there) plus the 2026-09-08 Validate report (architecture-validation-2026-09-08/ARCHITECTURE-VALIDATION.md, gate FAIL-conditional: 3 critical, 13 high, 13 medium, 8 low) and the 2026-09-08 PRD amendment (prds/prd-works-2026-06-14/.memlog.md, addendum handoff H1-H12)
- (constraint) AD-01..AD-25 stay binding with stable IDs; amend Rules in place, add AD-26+ for new decisions, never renumber or reuse; register wins over prose; legacy prose reconciled to the register, not rewritten
- (constraint) Inherited unchanged from the 09-06 run: platform host = Hexalith.Platform (AD-20), seam allocation generic->EventStore SDK first / domain->Works / topology->Platform, single Aspire 13.5.x family, FR-20 tiebreak = WorkItemId ordinal (AD-03), FR-10 and FR-11 unchanged
- (constraint) PRD 2026-09-08 amendment is the spec baseline (26 FRs incl. FR-26 Reactor; FR-6 full transition table; FR-15 non-matching resume = rejection; completion rules: explicit Complete at any Remaining contributes 0, unestimated contributes 0 + count, ReEstimate never completes; FR-12 Unit inheritance at spawn; FR-20 two-view predicate; section 9 identity-provenance NFR; actor = authenticated envelope acting party)
- (event) 2026-09-12 Update resumed from this memlog and architecture-works-2026-09-06/.memlog.md. Driving inputs: the 2026-09-12 validation report and its five lenses, the current PRD memlog (including the four post-validation overrides), the live Works/EventStore code, and the Hexalith baseline. Deliverable: architecture spine only. Mode: Fast path.
- (version) Reality check 2026-09-12: Works pins .NET SDK 10.0.400 while Microsoft's current .NET 10 servicing line is SDK 10.0.401/runtime 10.0.12; core Aspire is pinned at stable 13.5.3; CommunityToolkit.Aspire.Hosting.Dapr remains preview at 13.5.0-preview.1.260825-0345; central Dapr .NET packages are already 1.18.7; Works still hard-codes Dapr runtime 1.18.3 while official runtime 1.18.4 is current. Hexalith.Platform remains the target host, not current proof. Sources: live build files plus official .NET, NuGet, Dapr release, and Platform repository pages.
- (constraint) The 2026-09-08 PRD baseline governs 26 FRs and FR-26 makes Reactor the sole mechanical cross-aggregate coordinator. Later PRD-memlog overrides also bind this update: attach only after durable child creation; executor-responsibility actions require authenticated actor = current Executor; active Handoff is explicit/audited without status change; CorrectProgress is additive and may reopen only progress-auto-completed work.
- (decision) Amend AD-06/AD-22: persisted roll-up is one v3 document per (tenant, ancestor) at works:v3:tenant:<base64url-tenant>:rollup:<base64url-ancestor>. It contains topology watermarks keyed by descendant and contribution slots keyed by descendant; each topology watermark LWWs by registry envelope sequence and names ReservationId/Attached state, while each contribution slot LWWs by the descendant's EventStore envelope sequence and carries an absolute Works contribution snapshot. EventStore owns the ETag/CAS read-merge-retry and is the only durable writer; Works owns the pure merge/materialization function. Same-sequence unequal bytes quarantine/degrade; same-sequence equal bytes no-op. The same CAS commit preserves every other slot and materializes sorted per-Unit totals plus unestimated-descendant count. Binds: FR-11/R4. Prevents: whole-document overwrite, cross-stream sequence comparison, and released/quarantined attachments leaving live contribution slots.
- (decision) Amend AD-22: the fan-out source is not a raw event. After folding each accepted state-changing event, Works emits an absolute WorkContributionSnapshot carrying tenant/item, source envelope sequence, current status, sorted own Remaining-by-Unit, terminal-as-zero, and unestimated count. EventStore transports this opaque snapshot to every Attached ancestor and never interprets Works payloads. Binds: FR-11/SM-2. Prevents: delta-only ProgressReported and ReEstimated producing incompatible ancestor totals.
- (decision) Amend AD-21 under [ASSUMPTION]: registry identity is EventStore aggregate (tenant, domain work-tree, aggregateId registry), published on work-tree.events and consumed at /work-tree/events. Lifecycle is Reserved -> Creating -> Attached, or Reserved -> Released; a CAS-authorized Creating fence can no longer timeout-release. EdgeReserved carries full child-create payload; ChildCreateAuthorized mints stable ReservationId plus monotonic FencingToken; Reactor submits CreateWorkItem with both; a matching persisted WorkItemCreated is the sole attach evidence; EdgeAttached then drives the parent's token-bearing SpawnChild record. Released tokens are terminal. Creating failures are retried; audited abandonment may release only after proving no child creation evidence, otherwise it attaches/repairs. Binds: FR-13/16/26. Prevents: late commands creating ghosts, ChildSpawned-first attachment, and Attached edges without child streams.
- (decision) Amend AD-21/AD-20 R4: every topology consumer (fan-out, cascade, child-completion recovery, shared rebuild, repair) uses one Works-owned IWorkTreeTopologyReader contract over the registry read model; no consumer infers edges from WorkItem streams. ResolveAttachedAncestry requires the exact ReservationId and Attached registry sequence; Reserved/missing evidence is retryable and never acknowledged. EnumerateAttachedDescendants is tenant-scoped, cursor-paged, bounded, and stable. Works owns registry contracts/aggregate and pure Reactor translators; EventStore owns delivery, relationship-reader adapter, CAS projection, process/schedule primitives; Platform owns composition, timeout and MaxDepth policy. A tenant migration builds and verifies the registry before any consumer switches. Prevents: multiple topology truths and empty-ancestry acknowledgement.
- (decision) Add AD-26 under [ASSUMPTION]: every Reactor/reminder/recovery effect has EffectId = base64url(SHA-256(fixed-order length-prefixed UTF-8 tuple tenant, source domain, source aggregate, source envelope sequence, effect kind, target domain, target aggregate, effect ordinal)); both MessageId and IdempotencyKey reuse wrk_<EffectId>. Same key plus different canonical command digest is a conflict and quarantine. Target commands carry EffectId/reservation token and domain state or natural state-machine semantics deduplicates forever; EventStore terminal records are retained at least 90 days but are not the correctness boundary. Persist issuer workload, tenant delegation, purpose, and causation with the command. Binds: R11/FR-26. Prevents: duplicate cross-aggregate effects after retry, ownership migration, or retention expiry.
- (decision) Amend AD-16 under [ASSUMPTION]: one CAS-created epoch per tenant and projection family fences inventory capture through Commit. The works-runtime-v3 family includes roll-up, what's-next, registry topology, pending reminder indexes, and projection quarantine/parking. Every writer supplies the active epoch; stale/missing epoch writes are not acknowledged and retry after refresh. Begin seals inventory and source high-watermarks, Stage writes a complete candidate, Commit atomically promotes one manifest, then catch-up proves every post-capture source position acknowledged before the fence opens. Abort discards staging. Process-effect checkpoints are restored separately from backup and reconciled from source events/EffectIds before readiness. Binds: shared rebuild/R4. Prevents: acknowledged writes omitted by promotion and read-consistent projections with unrecoverable pending work.
- (decision) Amend AD-15 and migration rows R6/R7: use non-colliding canonical namespaces. Tenant keys begin works:v3:tenant:<base64url(UTF-8 canonical TenantId)>; control-plane keys begin works:v3:control and can never be formed from a tenant id. Canonicalize before validation/key creation. The control tenant index has one actor/CAS writer; checkpoint and index merges are monotonic, ETag-protected, cursor-paged, bounded, tombstone/prune aware, and audited. Replace the legacy raw tenants singleton and read/check/save checkpoint paths before migration. Prevents: reserved-name collision, lost checkpoint progress, unbounded enumeration, and offboarded tenants remaining live.
- (decision) Amend AD-23/AD-24 under [ASSUMPTION]: Platform issues a short-lived asymmetric-signed WorkloadDelegation JWT after authenticating the Dapr SPIFFE workload; claims bind issuer, audience eventstore-gateway, app id, tenant, purpose, source causation, EffectId, issued/expiry. EventStore validates signature/audience/expiry and exact SPIFFE app match, then enforces CommandOriginPolicy[(domain, command type)] before dispatch; allow-listed workloads never receive a global-admin bypass. External identity is OIDC plus Tenants membership. Responsibility actions ReportProgress, CorrectProgress, Complete, Suspend, and Handoff additionally require acting Party = current Executor; Claim binds the acting Party; registry SpawnChild, timer resume/expiry, cascade, repair, replay, and rebuild are limited to named workload/operator purposes. Direct-port, missing-carrier, wrong-app, wrong-tenant, wrong-purpose, and actor/executor mismatch tests fail closed. Prevents: cross-tenant confused-deputy and wrong-origin command execution.
- (decision) Amend AD-11/AD-25 under [ASSUMPTION]: reminder runtime identity is app works, actor type WorkItemReminderActor, actor id wrk_<hash(tenant,item)>; reminder state is a typed PendingWorkIntent with Kind DateResume|Expiry, tenant/item, due UTC instant, ScheduleToken, source sequence, and payload. EventStore owns generic registration/reconciliation; Works owns intent translation; Platform owns Scheduler HA/backup/policy. Effective expiry instant and ScheduleToken are persisted additively with create/reschedule facts; ExpireWorkItem echoes both and the aggregate rejects a stale mismatch. Reconciliation indexes by kind and tenant and never translates expiry as resume. Prevents: stale reminder expiry, actor-family divergence, and mixed intent recovery.
- (decision) Amend AD-13: while Suspended, only an exact current AwaitCondition resumes and clears the set; any nonmatch is a domain rejection without mutation. Only replay of the exact condition consumed by the successful resume is a no-op; every other post-resume or terminal condition rejects. Binds: FR-15 and lifecycle matrix. Prevents: adapters recording incompatible acknowledgement/checkpoint outcomes.
- (decision) Amend AD-04/AD-07/AD-10/AD-14/AD-17 for the current 26-FR contract: a child with no explicit Unit inherits its parent's Unit for its first estimate; explicit Complete from InProgress/Suspended contributes zero without changing Estimated/Done; progress reaching zero auto-completes; ReEstimate never completes; unestimated descendants contribute zero plus count; the Reactor owns eventual cross-aggregate effects and remains mechanical. Active Handoff emits WorkItemHandedOff and changes only ExecutorBinding in InProgress/Suspended. CorrectProgress emits ProgressCorrected and, only when it makes Remaining positive after a progress-caused completion, WorkItemReopened; explicit completion remains terminal. New WorkItemCompleted writes carry CompletionKind, with deterministic replay inference for historical events. Prevents: divergent projection/query/lifecycle behavior across the amended PRD.
- (decision) Add AD-27: recovery readers share one strict EventStore stream-page validator. FromSequence is exclusive, so the next request reuses LastSequenceReturned without +1; every page validates domain, canonical tenant, aggregate, strictly increasing envelope sequence, and state-affecting decode completeness. Recovery is durable and periodic, not startup-only: bounded hot retries transition to a durable unresolved/quarantine record that keeps works-recovery readiness degraded, emits bounded kind/reason metrics and alerts, and is retried until authenticated audited disposition. Ack occurs only after checkpoint/side effect or durable quarantine capture. Binds: AD-09/10/11 R7/R8. Prevents: page-boundary loss, silently stranded work, and Ready while recovery is incomplete.
- (decision) Add AD-28 under [ASSUMPTION]: Works classifies Obligation, notes, await/correlation values, and durable bodies as confidential tenant data; identifiers/causation are restricted operational metadata. Works owns classification/minimization; EventStore owns encryption-at-rest/in-transit, tenant-keyed retention/erasure mechanisms, and immutable event storage; Platform owns secret store/rotation, backup/restore, DR, and the append-only privileged-audit sink. Privileged repair/replay/rebuild/quarantine and authorization denials write audit records; sink failure blocks privileged mutation. No payloads enter logs/metrics/ProblemDetails. Production objectives are RPO <=15 minutes, RTO <=4 hours, quarterly restore drills; no non-synthetic shared data or new durable catalog type is admitted until retention/legal-hold/offboarding approval and restore proof exist. Prevents: irreversible incompatible privacy, erasure, and recovery choices.
- (decision) Add AD-29: additive durable-schema rollout is reader/validator/catalog/golden-corpus first, producer second; rollback retains new readers while new bytes exist. Unknown enum values and missing required security/fence fields fail closed; malformed or unknown state-affecting evidence is captured in one durable tenant-scoped quarantine and degrades the affected capability. Replay/disposition is authenticated, audited, and re-enters the same validation path. Child streams without reservation fields require the one-time registry migration; they are never silently treated as newly Attached. Binds: AD-01/21/25. Prevents: mixed-version writers bypassing fences and inconsistent quarantine behavior.
- (direction) Amend AD-19/AD-20: version sources are current-state specific. Until R1 migration, Works global.json owns .NET/Aspire SDK pins, Builds central props own package pins, and Works DaprSelfHostedMtls owns runtime images; after R1, Platform owns host SDK/runtime/topology pins. Core Aspire 13.5.3 is stable, but its selected Dapr hosting adapter is preview and allowed only in development/test unless Platform Maintainer records a time-bounded production exception with owner and stable-upgrade trigger. R4 fan-out, R6 durable reminders, and R7 process runner are absent producer work; R11 gateway exists. Works AppHost/ServiceDefaults remain until a Platform-owned R1-R11 parity lane and production profile are green.
- (direction) Execution gates carried in the spine: fix the two recovery cursor implementations and add multi-page/restart tests before recovery acceptance; create ordered transport-idempotency, registry, fan-out, expiry/reminder, Reactor-recovery, and Platform migration work; every R1-R11 row names producer artifact, minimum package/API contract test, consumer story, and proof command. Architecture-test proof is explicit assembly execution in CI, not solution compilation. VAL-H11 is closed by docs/eventstore-api-surface-constraints.md; UI/Fluent V5 remains deferred to first UI work.
- (decision) Clarification to AD-06/AD-22: every WorkContributionSnapshot writes the item's own roll-up document as a self slot and also each Attached ancestor. The self slot is eligible by exact item identity and needs no attachment watermark; all non-self slots require a matching Attached topology watermark. A nonterminal unestimated item contributes numeric zero and unestimatedCount=1; terminal contributes zero and count=0. Prevents roots/leaf documents being absent and self data being incorrectly gated on topology.
- (decision) PRD reconciliation fix: responsibility authorization also requires acting Party=current Executor for Reject and for Claim from Assigned; Claim from Queued binds the authenticated acting Party. Assign/ReEstimate/Reschedule/Cancel remain at the authenticated tenant-member floor unless later narrowed. Binds FR-6/17/18 and AD-23; prevents one member rejecting or stealing another Executor's assigned work.
- (constraint) Until prd.md/addendum.md are rendered from their latest memlog overrides, the PRD memlog's final four override entries are authoritative over contradictory rendered prose. Keep WorkItemCreated-gated attachment, executor responsibility checks, explicit active Handoff, and CorrectProgress conditional reopen in the spine; offer a companion PRD refresh rather than weakening the architecture.
- (decision) PRD reconciliation fix: AD-18 names IExpectationResolver and IExecutorRouter as Contracts-owned ports, ships the no-LLM expectation resolver, and leaves router unwired in v1; a compact owns/references table binds Parties, Conversations, Tenants, Commons, and EventStore. AD-16/22 serve no partial generation, mark prior generation stale/unavailable during repair/rebuild, and publish payload-free changed-key notifications only after durable commit. Platform resolves MaxDepth/TTL per tenant before admission; default MaxDepth=32 and breadth uncapped. Prevents missing ports, denormalized sibling data, stale-as-fresh reads, divergent notification payloads, and global-vs-tenant policy drift.
- (decision) Reconcile AD-26 with the live gateway: deterministic identifiers use SHA-256 encoded as 52-character uppercase Crockford Base32 and hyphen prefixes only; MessageId/IdempotencyKey = wrk-<digest>, satisfying EventStore's alphanumeric/hyphen validator. Every cross-aggregate success or rejection event persists EffectId plus canonical command digest, and replay retains the receipt map; repeated same digest no-ops, different digest quarantines even after gateway TTL/restore. Covers child create/record/registry transitions, date/child resume, expiry, cascade, and recovery. Prevents the proposed underscore/base64url ID from being rejected and closes permanent dedup ambiguity.
- (decision) Amend AD-21 fencing: Reserved may release only before ChildCreateAuthorized. Creating never becomes Released; operator intervention moves it to Quarantined while preserving the fence, and reconciliation later attaches if child evidence arrives or records an audited orphan disposition after proving a permanent target conflict. EventStore validates ReservationId/FencingToken against current registry state immediately before dispatch; stale/revoked/superseded tokens never reach Handle. Registry aggregateId=registry is an explicit reserved system-aggregate exception to edge ULIDs. Prevents manual abandonment racing an in-flight create.
- (decision) Amend AD-06/AD-16 key and writer protocol: the documented roll-up key is logical; physical family keys contain generation/epoch and the tenant manifest is the sole reader selector. During Building, refreshed ordinary writers durably append to the epoch catch-up journal and do not mutate active or staging documents; broker acknowledgement follows journal commit. Commit switches the manifest atomically, catch-up drains only into the promoted generation, readers mark it stale until drain proof, then the fence opens. Prevents fixed-key collision and loss of post-capture writes.
- (decision) Define reminder identifiers with the shared deterministic encoder: reminder actor id wra-<CrockfordBase32(SHA256(length-prefixed UTF-8 tuple reminder-actor, tenant, workItem))>; ScheduleToken wrs-<digest of schedule, tenant, item, effective-expiry UTC ticks, WorkItem state-change schedule revision>. Reminder name is date-<ScheduleToken> or expiry-<ScheduleToken>. Actor state stores the full tuple and a same-ID tuple mismatch quarantines. Registration, reconciliation, callback and aggregate golden tests must share vectors.
- (decision) Amend AD-23 issuer/binding: Hexalith.Platform Workload Delegation Service is sole JWT issuer and owns asymmetric signing-key rotation/JWKS through Platform secret management. Token binds full AD-26 source tuple, EffectId, target domain/aggregate, command type, and canonical command digest. EventStore recomputes all values and compares the JWT app claim with the mTLS/ACL-attested Dapr service-invocation dapr-app-id before origin-policy evaluation.
- (direction) AD-18 adopted scope is the current four-project DependencyDirectionTests allowlist only; executable/testing/Platform edges in the diagram are target rules and must be added to fitness before acceptance. Works Maintainer owns a tracked CI workflow that builds Release then executes dotnet tests/Hexalith.Works.ArchitectureTests/bin/Release/net10.0/Hexalith.Works.ArchitectureTests.dll; solution compilation alone is not proof.
- (decision) The spine uses explicit decision-state tags: ADOPTED means settled, CURRENT means verified in the tracked/checked implementation, TARGET means binding migration outcome not yet implemented, and ASSUMPTION means a fail-closed Fast-path choice awaiting user acceptance. This binds readiness claims and prevents target design from being mistaken for shipped behavior.
- (decision) WorkItem IDs remain backward-readable as any existing non-whitespace AggregateIdentity-compatible value and handlers never generate them; the target authenticated edge emits sortable ULIDs. This binds AD-02 migration and prevents invalidating current streams while eliminating future retry-time randomness.
- (version) The root-tracked Hexalith.Builds gitlink pins Dapr .NET packages 1.18.5; the locally advanced Builds checkout pins 1.18.7. Architecture evidence must distinguish reproducible tracked input from the current checkout and never silently treat the latter as committed root state.
- (decision) AD-20 R9 is partial/transitional because Works still owns a custom AppHost, ServiceDefaults, bespoke /project route, Dapr actor/reminder wiring, and local recovery. R11 has a gateway and trusted-intent API, but Works lacks the adapter, effect receipts, and Commit-retention binding. This binds migration truth and prevents false green parity.
- (decision) Cross-aggregate gateway terminal records use EventStore Commit retention rather than an invented 90-day tier; durable target effect receipts remain the correctness boundary and survive gateway optimization expiry. This binds idempotency retention and prevents relying on unavailable storage policy.
- (decision) A lifecycle contract change ships atomically with the transition matrix, catalog/validators, durable contracts, aggregate behavior, projection folds, golden corpus, and conflicting fitness/unit/integration tests. This binds handoff, correction, reopen, completion-kind, and unit-inheritance migration and prevents mixed-version behavioral authority.
- (decision) Recovery readiness requires one strict validator on live, replay, repair, and rebuild paths; durable unresolved records, periodic retry, bounded metrics/alerts, authenticated audited disposition, and degraded readiness persist until resolution. This binds AD-27 liveness and prevents quarantined work from becoming silent terminal loss.
- (decision) The current four-project dependency allowlist and existing architecture-test assembly are CURRENT; proposed Registry, Executable, Property, and Integration units are TARGET. The Works Maintainer owns a tracked CI step that explicitly executes the Release architecture-test assembly. This binds structural truth and prevents an aspirational seed from being reported as implemented.
- (decision) Responsibility authorization executes inside the serialized target aggregate turn after rehydration, using an immutable EventStore-issued AuthorizedCommandContext; no caller payload, read model, or edge-only check authorizes mutation. This binds actor identity to current Executor atomically and prevents handoff/claim races.
- (decision) Registry-authorized WorkItemCreated carries tenant, parent, child, ReservationId, FencingToken, and reserved-payload digest; ordinary parent-bearing Create is prohibited. Attached topology remains authoritative if the parent becomes terminal; token-bearing ChildSpawned is idempotent bookkeeping accepted in every parent status, and Cancelled/Expired parents schedule cascade catch-up before attachment processing acknowledges. This binds terminal-parent races and prevents valid children becoming orphans.
- (decision) One versioned EventStore-owned deterministic codec defines canonical text, field order, effect identifiers/ordinals, binary tuples, semantic command bytes, and AD-11/25/26 hashes. EventStore owns a private target-scoped effect inbox; receipt and success/rejection/no-op disposition commit atomically with target persistence, never as a Works domain event. This binds canonicalization and receipt atomicity and prevents independent producer encodings or partial dedup commits.
- (decision) Effect ordinal is an immutable catalog value by source event, effect kind, and target role, never traversal order. Effect receipts share the tenant legal-hold/offboarding class; receipts and source/target evidence erase only as one authorized tenant operation, and evidence below the retained source floor rejects/quarantines rather than re-executes. This binds deterministic fan-out and retention.
- (decision) EventStore is sole rebuild epoch/journal writer. BeginBuilding requires every active writer to hold the required protocol lease; every commit validates epoch/lease, and stale writers fail without acknowledgement. Journal identity is tenant, epoch, family, source stream, envelope position with digest conflict detection; journal and consumer checkpoint commit atomically. Abort drains journal to active before clearing staging; promotion stays stale until promoted catch-up drains. This binds mixed-version fencing and crash safety.
- (decision) WorkContributionSnapshot is a non-domain projection message derived only from a committed EventStore envelope, never appended to aggregate streams or counted in payload Sequence. Works.Projections owns fold/message; EventStore owns generic merge/relationship SPI; the Works executable alone composes the adapter. This binds port direction and prevents shared runtime depending on Works.
- (decision) TenantId is canonical only when already lowercase ASCII kebab-case, 1..64 characters, matching ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$; trust boundaries reject rather than transform and reuse exact UTF-8 bytes everywhere. This binds claims, envelopes, references, keys, hashes, and equality and prevents alias tenants.
- (decision) Production registry admission requires a Platform-supplied per-tenant topology quota; missing or exhausted quota fails before EdgeReserved. Breadth has no domain cap below that operational quota. EventStore snapshots the registry, bounds command replay, exposes saturation/backpressure, and proves configured-quota latency. This binds capacity safety and prevents one unbounded registry stream from becoming a tenant denial of service.
- (decision) AD-03 separates current and target query behavior: the current query returns a tenant's Assigned plus Queued items in deterministic order; optional Executor PartyId filtering is adopted target behavior because the live handler reads only TenantId. R5 remains Partial until the executor filter and proof exist. This binds brownfield truth and prevents an unimplemented filter being reported available.
- (version) Aspire 13.5.3 is an atomic dual pin in root global.json and src/Hexalith.Works.AppHost/Hexalith.Works.AppHost.csproj; BuildConfigurationTests enforces equality and the project SDK attribute selects the AppHost build. After R1 the equivalent Platform pin set becomes authoritative.
- (version) Source provenance on 2026-09-12: EventStore package 3.103.0; root-tracked EventStore gitlink 6b0247ac versus advanced checkout a568af4e, with reviewed APIs unchanged; root-tracked Builds a32cb422 versus advanced checkout fa647278, changing Dapr SDK packages 1.18.5 to 1.18.7. This prevents package version claims from masking source checkout drift.
- (decision) Production readiness closes servicing and compatibility gaps by upgrading or recording a time-bounded accountable risk acceptance for the exact .NET/Aspire/CommunityToolkit/Dapr tuple, then proving restore, Release build, focused integration, and Platform parity. This binds current pin gaps and prevents compile-only evidence from becoming production approval.
- (decision) Terminal-safe child bookkeeping preserves FR-16 optional suspension: after exact attachment, internal SpawnChild always records ChildSpawned; when the reserved flag is true it suspends an InProgress parent or unions the child-completed AwaitCondition into an already Suspended parent, while terminal parents remain terminal and Cancelled/Expired schedule cascade catch-up. This binds the reservation payload across parent races and prevents universally state-neutral bookkeeping from dropping child waits.
- (decision) The registry reserved-payload digest uses the same versioned AD-26 canonical codec and golden corpus. This binds authorization evidence to effect encoding and prevents Registry and EventStore from hashing the same child payload differently.
- (decision) Canonical TenantId is validated, not normalized: trust boundaries reject noncanonical input and never silently transform it. This binds AD-15 wording and prevents two apparent canonicalization policies.
- (decision) A duplicate AD-26 key with the same digest returns the recorded success, rejection, or no-op disposition without redispatch, rather than merely reporting a generic no-op. This binds replay observability and prevents retry callers receiving a different outcome.
- (decision) Target Claim carries no caller-provided ExecutorBinding: EventStore builds it from the immutable authorized actor plus tenant-resolved Channel and AuthorityLevel; Assigned claim additionally matches current Executor, and Queued claim binds that derived context. This binds responsibility identity and prevents a claimant rebinding the item through payload fields.
- (decision) Registry reservation uses an EventStore-issued ParentAdmissionWitness obtained inside the serialized parent turn immediately before registry dispatch. The witness binds tenant, parent, child, reserved-payload digest, suspend flag, and parent envelope sequence. suspend=false requires a nonterminal parent; suspend=true requires InProgress or Suspended, so Created/Assigned/Queued reject before EdgeReserved. Later InProgress/Suspended/terminal races follow the attachment bookkeeping rules. This binds FR-16 status admission and prevents incompatible handling of pre-active parents.
- (event) spine finalized
