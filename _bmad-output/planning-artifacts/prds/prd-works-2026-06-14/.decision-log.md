# Decision Log — Hexalith.Works PRD

Canonical memory and audit trail for this PRD. Every decision, change, and override is recorded here as the conversation unfolds.

## 2026-06-14 — Session start

- **Intent:** Create (no prior PRD existed under `_bmad-output/planning-artifacts/prds/`).
- **Product:** Hexalith.Works — work-item coordination kernel; domain module in the Hexalith umbrella repo (`works`).
- **Primary inputs read in full:** `briefs/brief-works-2026-06-14/brief.md` (final), `addendum.md`, `.decision-log.md`.
- **Persistent facts loaded:** `Hexalith.Projects/_bmad-output/project-context.md` (ecosystem rules: .NET 10, Dapr-only infra abstraction, event-sourced EventStore with persist-then-publish + canonical `{tenant}:{domain}:{aggregateId}` identity, mandatory tenant isolation, Contracts/Client/Server/Projections package layout, PolymorphicSerializations for events). Repo `CLAUDE.md` (Works = domain code only; technical concerns factored to shared modules; root submodules only).
- **Known referenced input not yet ingested:** `_bmad-output/brainstorming/brainstorming-session-2026-06-14-0910.md` (44 ideas, 6 themes — the source of record for domain ideas).

### Inherited scope from the brief (treated as settled unless revisited)

- **v1 = Themes 1 & 2:** Work Item Essence (the `WorkItem` aggregate) + Thin-Core Architecture & Boundaries.
- **Deferred (designed-for, built later):** Theme 3 LLM-native interaction / magic links, Theme 4 executor routing & escalation, Theme 5 cost burn-down & spend governance, Theme 6 trust/security hardening. Ports (`IExpectationResolver`, `IExecutorRouter`), signed-act audit model, and cost-ready burn-down are laid in v1.
- **Keystone bets:** everything-is-a-Party executor binding; dual effort+cost burn-down on one tree; AI in the loop, never in the system-of-record.

### Calibration answers (2026-06-14)

- **Working mode:** Fast path — facilitator batches gaps into 1–2 consolidated questions, drafts the full PRD with `[ASSUMPTION]` tags on inferred gaps; user reviews and corrects.
- **Scope:** v1 (Themes 1 & 2) as buildable, numbered FRs **plus** a "designed-for / roadmap" section documenting Themes 3–6 seams (ports, signed-act audit, cost-ready burn-down) without specifying them as v1 work.
- **Brainstorm:** pull in fully — subagent-extract all 44 ideas so none are dropped from the FRs.
- **Research:** no fresh landscape research — addendum's competitive digest is dated 2026-06-14 and sufficient.

### Brainstorm extraction (2026-06-14)

- Subagent extracted all **44 ideas / 6 themes** from `brainstorming-session-2026-06-14-0910.md`. v1 candidates (Themes 1 & 2): aggregate fields, lifecycle state machine, named domain events (`WorkItemCreated`, `ProgressReported`, `ReEstimated`, `ChildSpawned`, `Suspended`, `Resumed`, `Completed`, …), recursive roll-up projection (cost-ready), executor-binding value object, boundary decision record, ports (`IExpectationResolver` no-LLM-first, `IExecutorRouter`), Aspire host.
- **Gaps the brainstorm/brief left open** (carried into Fast-path question or `[ASSUMPTION]`): (1) v1 surface boundary — kernel-only vs. thin non-LLM CLI/MCP command surface; (2) scheduling/"what's next" — fields only vs. a ranking query (thin-core vs. "competes for attention" tension); (3) await-condition trigger kinds in v1; (4) success-metric rigor; (5) roll-up consistency + tree depth/cycle safeguards [→ ASSUMPTION]; (6) authority-level enum [→ ASSUMPTION]; (7) rejection/expiry semantics [→ ASSUMPTION]; (8) expectation contract authored-vs-inferred representation [→ ASSUMPTION, Theme 3-leaning].

### Consolidated Fast-path gap answers (2026-06-14)

- **v1 surface:** Kernel + Aspire test host only. No production adapters; MCP/CLI/email deferred. v1 proves the model through the Aspire-hosted test harness exercising the command pipeline.
- **Scheduling:** Fields (priority + due date) **plus** a read-side "what's next" query projection ordering claimable/assigned work by priority+due. No routing/assignment engine (Theme 4).
- **Await triggers:** v1 natively handles child-completion resume + park-until-date/timer; external (webhook/reply) resumes via a generic resume-by-correlation-id port, concrete adapters deferred (Theme 3).
- **Acceptance:** Build-signal acceptance criteria (E2E lifecycle, correct roll-up, zero executor-type branching, green build+tests under Aspire). NFRs stated qualitatively, no numeric targets pinned in v1.

### Draft 1 produced (2026-06-14, Fast path)

- `prd.md` filled — Essential Spine + adapted sections. Structure: §0 Purpose, §1 Vision, §2 Target User (JTBD, Non-Users v1, 4 lean UJs — builder/system-centric + 1 deferred end-user horizon), §3 Glossary, §4 Features (7 features, **FR-1…FR-25**), §5 Non-Goals, §6 MVP Scope, §7 Why Now, §8 Public Surface & Compatibility (dev-product cluster), §9 Cross-Cutting NFRs, §10 Constraints & Guardrails (seams laid in v1), §11 Success Metrics (build-signal SM-1…SM-5 + counter-metrics SM-C1/C2), §12 Roadmap/Designed-For (Themes 3–6 table), §13 Open Questions (8), §14 Assumptions Index.
- `addendum.md` created — inherited substrate constraints (detailed), non-binding event/port sketch for the architect, deferred-theme mechanism-depth pointer.
- **Inferred decisions tagged `[ASSUMPTION]`** (13, indexed in §14) and **8 Open Questions** raised — chiefly: unestimated completion, Expectation representation, AuthorityLevel set, reject/expire semantics, tree-depth guard (proposed 32), roll-up consistency (proposed eventual), heterogeneous-unit roll-up, routing-decision event placeholder.
- Awaiting user review/redline of the `[ASSUMPTION]` and Open-Question areas before Finalize.

### User review (2026-06-14) — assumptions accepted

- User responded "ok" to the draft-1 review offer (which recommended walking Q3–Q6). Interpreted as **acceptance of the 13 `[ASSUMPTION]` calls and the proposed answers to all 8 Open Questions as drafted.** Specifically confirmed-by-acceptance: unestimated-completion rule (Q1), Expectation = structured/empty default resolved on demand (Q2), AuthorityLevel `{ Read, Contribute, Coordinate, Administer }` carried-not-enforced (Q3), reject/expire semantics (Q4), tree-depth guard default 32 (Q5), eventual-consistency roll-up (Q6), per-Unit-subtotal heterogeneous roll-up no conversion (Q7), no v1 event placeholder for Theme 4 routing-decision record (Q8). Closed the 13-event v1 catalog (FR-7) and MCP/CLI fully out of v1 confirmed.
- Proceeding to **Finalize**: decision-log audit → input reconciliation → reviewer pass → triage → polish → close.

### Finalize (2026-06-14)

- **Step 1 — Decision-log audit:** all session decisions trace into `prd.md`/`addendum.md` (accounting below). No orphaned decisions.
- **Steps 2 & 3 — Input reconciliation + Reviewer gate:** dispatched as parallel subagents (reconcile brief, reconcile brainstorm; rubric walker, architecture-readiness reviewer). `finalize_reviewers` empty — no adversarial-general by default; one ad-hoc readiness reviewer added because the PRD feeds the architecture phase. Reviews saved: `review-rubric.md`, `review-readiness.md`, `reconcile-brief.md`, `reconcile-brainstorm.md`.
  - **Verdicts:** rubric = **Strong** (0 crit/0 high, 4 med/5 low, all 7 dimensions strong); readiness = **Ready-with-fixes** (2 crit/7 high); reconcile-brief = 2 high; reconcile-brainstorm = no Theme-1/2 drop, 5 med.

- **Step 4 — Triage (disposition):** AUTOFIXED in-PRD (behavioral/requirement-level): parent cancel/expire→child **cascade** (FR-10, was crit); added `WorkItemRescheduled` event (FR-7/FR-9); state-machine bidirectional/terminal edges (FR-6); `WorkItemQueued` requeue overload (FR-18); **single-claim-wins** concurrency rule + NFR (FR-18/§9); roll-up on substrate projection infra + idempotent/out-of-order (FR-11); resume = command keyed to Await-Condition, timers/external as commands keeping `Handle` clock-free (FR-15, removed phantom port); **multiple Await-Conditions** resolve-on-first-match (Glossary/FR-5, fixes UJ-3 contradiction); reject-default-requeue + expire-Due-Date/TTL (FR-10); **single-tenant tree** + breadth note (FR-13); query-side authz NFR (§9/§20); projection rebuildability NFR (§9); Conversation-ID linkable (FR-21); idempotency mechanism clarified (§10); `meter`/`Resumed`-status Glossary fixes; complete-without-estimate path (FR-8); event-stream-is-narrative + optional note (FR-7); SM-1 restart/rehydrate step. RECONCILIATION fixes: end-user problem restored as co-primary (§2.1/§2.2); omnichannel "seam-on-day-one, adapter-later" reconciliation note (§2.2/§5); no-moat honesty (§7); answer-contract triple-duty (§10); one-ladder-both-ways framing (§12); MCP/CLI brainstorm-Theme-2 deliberate-deferral note (§5); cost-aware-scheduling tension (§12). OQ-8 routing-record resolved via additive-evolution rationale (§5/§12).
  - **DEFERRED to architecture (logged, not PRD-bloat):** aggregateId derivation, concrete Priority type/ordering, optimistic-concurrency ETag mechanism, timer/scheduler adapter design, projection rebuild ops, validation domains (ProgressReported delta / Unit immutability / TTL source) — now §13 Open Questions (re-scoped to mechanism-only).
  - Draft OQ-1…OQ-8 **resolved by user acceptance**; moved to §14 Assumptions Index (all marked confirmed 2026-06-14).
- **Step 5 — Polish:** `doc_standards` applied via subagents in declared order, parallel across docs / sequential within: structure (`bmad-editorial-review-structure`) then prose (`bmad-editorial-review-prose`). prd.md: 8 structural edits (incl. stale §15→§14 cross-ref fix) + 6 prose edits; addendum.md: 1 structural + 1 prose. All FR-1…FR-25, SM-1…SM-5/SM-C1/C2, UJ-1…UJ-4, §0…§14, and all `[ASSUMPTION]` tags verified intact after polish.
- **Step 6 — External handoffs:** none configured (`external_handoffs` empty) — local files only.
- **Step 7 — Close:** `status: draft → final` on `prd.md` and `addendum.md` (`updated: 2026-06-14`). PRD session complete.
- **Artifacts:** `prd.md` (final), `addendum.md` (final), `.decision-log.md`; reviewer/reconcile working files retained (`review-rubric.md`, `review-readiness.md`, `reconcile-brief.md`, `reconcile-brainstorm.md`) for drill-in.
- **Recommended next:** `bmad-create-architecture` (the PRD is written to feed it; §13 lists the mechanism decisions awaiting it) → `bmad-create-epics-and-stories`. `bmad-ux` is low-value for v1 (headless kernel; UX matters when Theme 3 channel surfaces are built).
