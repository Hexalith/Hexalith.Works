# Architecture Validation Report — Hexalith.Works — 2026-09-08

> **Gate verdict: FAIL, conditional.** The register is a safe substrate for the sprint in flight (Story 4.8 close-out and Story 4.9 entry), but it is **not yet a safe substrate for the three stories it exists to enable** — the Work-Tree Registry (AD-21), the registry-backed roll-up fan-out (AD-22), and expiry reminders (AD-25). Three critical holes inside that new protocol let a Works-built unit and an SDK/platform-built unit both obey every AD and still write different roll-up documents, compute different numbers, and disagree on who creates a child. Thirteen high findings cluster around acceptance ownership (Story 4.9 carries none of the acceptance the register pins on it), identity/origin enforcement points, and drift since the register was bound.

## Scope and evidence

This Validate run assessed [`architecture.md`](../architecture.md) (1197 lines, `updatedAt: 2026-09-06`) as an architecture spine. The binding surface is the **Architecture Decision Register** (AD-01…AD-25, legacy-label map, open-findings register; lines 26–455); the sections below it are legacy analysis and seed, and the document states that the register wins where they disagree. The source was **not modified**. Its SHA-256 at review time is `2eff9c10fdd92c3929af71389139b304a2f02690deee9272dd93b0ad56d66e7d`; repository HEAD was `0527d12` with uncommitted Story 4.8 edits in the working tree.

The gate combined deterministic lint with five independent review lenses, each run in a fresh context against the same file. Raw counts overlap across lenses and must not be summed as distinct defects; the deduplicated row is the report.

| Input | Critical | High | Medium | Low | Lens verdict |
| --- | ---: | ---: | ---: | ---: | --- |
| [Rubric walker](reviews/review-rubric-walker.md) | 0 | 5 | 5 | 4 | Pass with concerns (4/9 checklist items clean) |
| [Adversarial divergence](reviews/review-adversarial-divergence.md) | 3 | 5 | 6 | 1 | **Fail** |
| [Technology reality](reviews/review-technology-reality.md) | 0 | 1 | 5 | 5 | Pass with concerns |
| [Security & data integrity](reviews/review-security-data-integrity.md) | 0 | 4 | 7 | 2 | Accept with concerns |
| [Code & spec conformance](reviews/review-code-spec-conformance.md) | 0 | 3 | 7 | 5 | Pass with concerns (15 ADs ratified, 4 drift, 6 not yet built) |
| [Deterministic lint](lint-spine.json) | 0 | 0 | 0 | 10 | Ten false positives |
| **Deduplicated report** | **3** | **13** | **13** | **8** | **Fail, conditional** |

The ten lint flags are brace-token checks for `{tenant}`, `{domain}`, and `{id}` at source lines 448, 473, 492, 666, 756, and 984. They are intentional key-shape notation, the same class dispositioned *ignore* at the 2026-09-05 and 2026-09-06 gates. Decision-shape lint is clean: no duplicate or non-monotonic `AD` IDs, no `AD` missing Binds / Prevents / Rule, no placeholder markers.

### What improved since 2026-09-05

The five criticals of the previous gate (VAL-C01…C05) are **closed at the invariant level**: the platform host is named with an owner and a seam-by-seam matrix (AD-20); tree attachment has an authoritative single-writer owner (AD-21); live roll-up has a delivery design (AD-22); identity provenance and trusted origin are bound as falsifiable rules (AD-23, AD-24); expiry has a trigger owner (AD-25). VAL-H02/H03/H04/H05 are closed. All named technologies exist and every pinned version is current on the web as of today. The residue of this gate is almost entirely *inside* the new entries — protocol details two repositories will build against — plus the acceptance bookkeeping that makes those entries land.

## Critical findings

### VAL2-C01 — The persisted roll-up document and its merge function are unbound; two writers will collide on one key

- **Issue:** AD-06 binds the *logical* shape (one LWW slot per descendant, keyed by the descendant's own sequence) and AD-22 assigns the multi-document fan-out to the EventStore SDK while "Works owns the pure contribution/merge strategy". Neither names the persisted document type, the per-slot merge function, the seam signature the SDK calls, or that a document-level watermark is forbidden. The live document carries exactly such a watermark (`LatestAcceptedSourceSequence`) and the live dispatcher does document-level LWW; the live strategy still computes the rolled value by recursive in-memory traversal. The register presents the flattened slot document as `[ADOPTED]` when it is the AD-22 target.
- **Divergence/risk:** An SDK fan-out that writes slots into an ancestor's key and a Works single-aggregate dispatch that keeps the higher-watermark document compare sequences across two streams — the precise failure AD-06 exists to forbid — and one overwrites the other wholesale. The RR-1 convergence property cannot hold across the two writers.
- **Source:** [`architecture.md:82–95`](../architecture.md#L82) (AD-06), [`:318–342`](../architecture.md#L318) (AD-22), [`:263`](../architecture.md#L263) (R4), [`:465`](../architecture.md#L465) (retired formula still printed).
- **Corroborating evidence:** [`WorkItemRollUp.cs:29–38`](../../../src/Hexalith.Works.Contracts/Models/WorkItemRollUp.cs#L29) (single watermark, no slot map); [`WorkItemProjectionDispatcher.cs:446–457`](../../../src/Hexalith.Works/Projections/WorkItemProjectionDispatcher.cs#L446) (document-level LWW); [`WorkItemRollUpProjection.cs:234–265`](../../../src/Hexalith.Works.Projections/Strategies/WorkItemRollUpProjection.cs#L234) (recursive fold); [`docs/work-roll-up-projection.md:94–100`](../../../docs/work-roll-up-projection.md#L94); [`IDomainProjectionHandler.cs:11–18`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/IDomainProjectionHandler.cs#L11) (stateless single-read-model seam, no fan-out contract to inherit). Registry-driven slot removal on detach/cancel is also impossible under "written only from its own descendant's stream" (ADV-11).
- **Found by:** Adversarial ADV-01, ADV-11; conformance CONF-11; security SEC-07 (provenance check location).
- **Disposition:** **Autofix in Update** for the register text (declare the persisted `WorkItemRollUpDocument` shape in Contracts with per-field merge: own fields LWW by own sequence, each slot LWW by its descendant's sequence only, no document-level gate; declare the `IRollUpSlotMerge`-style seam Works supplies and the SDK calls without comparing sequences; split AD-06 into adopted invariant vs target storage shape). **Discuss** which story replaces the live document. No change applied.

### VAL2-C02 — "Own contribution" is undefined and "computable from the event alone" is false

- **Issue:** Everything in AD-06/AD-22 rests on "that descendant's own contribution per Unit", yet no AD defines the function, and AD-22 asserts the value is computable from the event alone. `ProgressReported` carries a delta; terminal → 0 needs status; a mismatched-Unit `ReEstimated` is retained-not-applied and unknowable from the event. The PRD amended today now also says explicit completion at Remaining > 0 contributes 0, unestimated items contribute 0 plus a count, and re-estimate below Done clamps. AD-07 still says only "Remaining 0 → Completed".
- **Divergence/risk:** With the fan-out writer in the EventStore repository and the strategy in Works, the two compute different numbers for any item with prior progress, a re-estimate, a terminal transition, or mixed-Unit history — silent numeric corruption of every ancestor total (the SM-2 failure AD-06 exists to prevent), undetectable unless both units are tested together.
- **Source:** [`architecture.md:82–95`](../architecture.md#L82), [`:97–103`](../architecture.md#L97) (AD-07), [`:318–326`](../architecture.md#L318) (AD-22 premise), [`:336–337`](../architecture.md#L336).
- **Corroborating evidence:** [`WorkItemEffort.cs:26–36`](../../../src/Hexalith.Works.Contracts/ValueObjects/WorkItemEffort.cs#L26); [`WorkItemRollUpProjection.cs:222–232`](../../../src/Hexalith.Works.Projections/Strategies/WorkItemRollUpProjection.cs#L222) (terminal → 0 from folded state); [`WorkItemState.cs:176–201`](../../../src/Hexalith.Works.Contracts/State/WorkItemState.cs#L176); [`prd.md:202–203, 223, 239`](../prds/prd-works-2026-06-14/prd.md#L202) (2026-09-08 completion rules).
- **Found by:** Adversarial ADV-02; rubric walker RW-05.
- **Disposition:** **Autofix in Update.** Add to AD-06 Binds: own contribution = clamped `Remaining` of the descendant's own effort, per Unit; 0 for any terminal status including explicit completion at Remaining > 0; 0 plus an unestimated count for an item with no estimate. Amend AD-22: the contribution slot is produced only by the descendant's own single-aggregate projection from folded state and *transported* by the fan-out, which never inspects Works payloads; a slot is emitted on every state-changing delivery including terminal transitions. Amend AD-07 for explicit completion. No change applied.

### VAL2-C03 — Nobody owns the child-creation act; the freshness witness has no carrier; topology still has three sources of truth

- **Issue:** AD-21 marks an edge `Attached` on `ChildSpawned` evidence but `Released` on *child-stream* evidence — two different evidence sources in one entry. Today `SpawnChild` emits `ChildSpawned` and nothing creates the child aggregate (projections fabricate the node); the PRD addendum says "the pipeline then creates the child" with `CreateWorkItem`. AD-22's freshness witness ("creation evidence records the registry sequence of its reservation") has no field on any type in the chain, and the register does not say whether it is the envelope `SequenceNumber` or the payload ordinal. Meanwhile `CreateWorkItem.Parent` (with caller-fed ancestors, depth, and `MaxDepth`) remains a **public** command field the registry never sees, while stream-reading recovery sources and the shared-rebuild manifest still discover edges from `WorkItemCreated.Parent` and `ChildSpawned`.
- **Divergence/risk:** A registry built to release on missing child-stream evidence auto-releases every healthy edge a kernel that never streams the child creates; the child node gets two equal-sequence writers; the witness lands on `ChildSpawned` in one unit and `WorkItemCreated` in another; roots created with `Parent=P` by an ordinary tenant member are invisible to registry-driven fan-out and cascade but visible to stream-reading resume and rebuild — divergent trees per consumer, and "single-parent" enforced in a registry the item stream contradicts.
- **Source:** [`architecture.md:272–316`](../architecture.md#L272) (AD-21, esp. :279–282, :293–299, :303–307, :308–316), [`:327–333`](../architecture.md#L327) (AD-22 witness), [`:791`](../architecture.md#L791) (two counters), [`:377–383`](../architecture.md#L377) (AD-24 origin rule covers `SpawnChild` only).
- **Corroborating evidence:** [`WorkItemAggregate.cs:71–150`](../../../src/Hexalith.Works.Server/Aggregates/WorkItemAggregate.cs#L71) (`SpawnChild` creates nothing) and [`:12–66`](../../../src/Hexalith.Works.Server/Aggregates/WorkItemAggregate.cs#L12) (`CreateWorkItem` persists caller-fed `Parent`); [`WorksHost.cs:70–84`](../../../src/Hexalith.Works/Runtime/WorksHost.cs#L70) (no `ChildSpawned` handler); [`WorkItemRollUpProjection.cs:112–123, 150–157`](../../../src/Hexalith.Works.Projections/Strategies/WorkItemRollUpProjection.cs#L112) (fabricated child node); [`CreateWorkItem.cs`](../../../src/Hexalith.Works.Contracts/Commands/CreateWorkItem.cs); [`StreamReadingChildCompletionAwaitingParentSource.cs:41–50`](../../../src/Hexalith.Works/Recovery/ChildCompletion/StreamReadingChildCompletionAwaitingParentSource.cs#L41); [`StreamReadingCascadeDescendantSource.cs:62–72`](../../../src/Hexalith.Works/Recovery/Cascade/StreamReadingCascadeDescendantSource.cs#L62); [`WorkItemSharedRebuildManifestBuilder.cs:167–184`](../../../src/Hexalith.Works/Projections/SharedRebuild/WorkItemSharedRebuildManifestBuilder.cs#L167); [`addendum.md:41`](../prds/prd-works-2026-06-14/addendum.md#L41); [`docs/work-tree-shape-guard.md:35–42`](../../../docs/work-tree-shape-guard.md#L35).
- **Found by:** Adversarial ADV-03, ADV-08; rubric walker RW-07; conformance CONF-08.
- **Disposition:** **Discuss, then autofix.** Bind a single creation act (recommended: the Reactor translates `ChildSpawned` → `CreateWorkItem(child, Parent, AttachmentSequence)`; `WorkItemCreated` gains an additive `AttachmentSequence` field that is the AD-22 witness and the only creation evidence the registry accepts for `Reserved → Attached`); restrict `CreateWorkItem` with a non-null `Parent` or any ancestor/depth/policy field to the Reactor's workload identity (extend AD-24); make the registry the sole enumerator for fan-out, cascade, child-completion discovery, and rebuild inventory with stream facts reserved for repair; define a one-time registry seeding migration. Also fix `addendum.md:41`. No change applied.

## High findings

### VAL2-H01 — AD-13's resume rule contradicts the authoritative transition matrix, the PRD, and the code

- **Issue:** AD-13 says a `ResumeWorkItem` matching no current condition is a no-op. AD-17 makes `docs/lifecycle-transition-matrix.md` authoritative, and that matrix, PRD FR-15 (2026-09-08), Story 3.5, and the aggregate all make it a domain rejection; only a repeat of the consumed condition is a no-op.
- **Divergence/risk:** Two register entries disagree, so "register wins" cannot resolve it. Adapter builders (expiry, external-signal, R6 reconciler, R7 runner) reading AD-13 expect silence; the aggregate persists a rejection. `DateReminderActor.cs:73–74` already encodes the AD-13 assumption, true only when the same `DateReached` repeats.
- **Source:** [`architecture.md:159–164`](../architecture.md#L159), [`:196–200`](../architecture.md#L196), [`:219`](../architecture.md#L219), [`:811–813`](../architecture.md#L811), [`:838–840`](../architecture.md#L838).
- **Corroborating evidence:** [`lifecycle-transition-matrix.md:165–174`](../../../docs/lifecycle-transition-matrix.md#L165); [`WorkItemAggregate.cs:229–240`](../../../src/Hexalith.Works.Server/Aggregates/WorkItemAggregate.cs#L229); [`DateReminderActor.cs:73–74`](../../../src/Hexalith.Works/Reminders/DateReminderActor.cs#L73); [`prd.md:285`](../prds/prd-works-2026-06-14/prd.md#L285); [`epics.md:191–193`](../epics.md#L191) (NFR-9 inherited the wrong wording).
- **Found by:** Rubric walker RW-01; conformance CONF-05.
- **Disposition:** **Autofix in Update.** Rewrite the AD-13 Rule to the matrix's semantics and propagate to the prose; flag epics NFR-9 to the PM. No change applied.

### VAL2-H02 — Story 4.9 carries none of the acceptance the register pins on it

- **Issue:** AD-23 and AD-24 make their enforcement and negative tests "Story 4.9 platform acceptance"; AD-20 gates removal on matrix rows going green; VAL-H06/H07/H08/H09 revisit at "Story 4.9 R-n acceptance". Story 4.9's seven acceptance criteria mention none of OIDC, claim-derived tenant, deny-before-dispatch, mTLS negatives, forged `SequenceNumber`, direct-`SpawnChild` denial, the positive production-policy reminder test, or the R4/R6/R7/R8 proofs. Its prerequisite paragraph still says the architect must name the host. Its AC6 ("architecture tests fail when a Works-owned hosting project is introduced") is currently inverted by `BuildConfigurationTests`, which ratifies the Works AppHost.
- **Divergence/risk:** The platform maintainer satisfies every AC while leaving AD-23/AD-24 unimplemented and four open items unbound; the register then treats them as closed at "4.9 acceptance". The PRD's new §9 identity-provenance NFR has no story.
- **Source:** [`architecture.md:250–252`](../architecture.md#L250), [`:366–368`](../architecture.md#L366), [`:390–395`](../architecture.md#L390), [`:445–448`](../architecture.md#L445).
- **Corroborating evidence:** [`epics.md:1335–1391`](../epics.md#L1335) (Story 4.9), [`:1381`](../epics.md#L1381) (stale prerequisite); [`addendum.md:66`](../prds/prd-works-2026-06-14/addendum.md#L66) (H6 reaches the same conclusion); [`BuildConfigurationTests.cs`](../../../tests/Hexalith.Works.ArchitectureTests/FitnessTests/BuildConfigurationTests.cs); [`WorksMtlsAuthorizationSmokeTests.cs:27`](../../../tests/Hexalith.Works.IntegrationTests/WorksMtlsAuthorizationSmokeTests.cs#L27) (one allow/deny fact, Works lane).
- **Found by:** Rubric walker RW-02; conformance CONF-02, CONF-04.
- **Disposition:** **Autofix in epics (via Update or correct-course):** append AD-20 matrix proofs and AD-23/AD-24 acceptance blocks to Story 4.9 citing AD IDs; replace the stale prerequisite; extend the VAL-M01 row so the fitness lane flips in the same change that removes the projects. **Discuss** whether the negative suite is a separate platform-lane story. No change applied.

### VAL2-H03 — AD-25 automatic expiry has neither code nor an owning story; AD-21 and AD-22 have no stories either

- **Issue:** FR-10 is v1 scope. AD-25 binds a durable expiry reminder registered from lifecycle events. Nothing implements it, no story in `epics.md` or `sprint-status.yaml` owns it, Story 2.5 covers only the command, and matrix row R6 lists "expiry reminders (AD-25)" among seams "today in Works". The registry (AD-21) and fan-out (AD-22) likewise have no story; the Epic 3 note only sequences VAL-H10 ahead of a registry story that does not exist.
- **Divergence/risk:** A conformant v1 ships no automatic expiry — the VAL-H01 divergence AD-25 claims to have resolved — while the register says "FR-10 stands unchanged, no correct-course pass is required". AD-25's Prevents is only prevented if someone is tasked to build it.
- **Source:** [`architecture.md:397–416`](../architecture.md#L397) (AD-25), [`:265`](../architecture.md#L265) (R6 wording), [`:1109–1110`](../architecture.md#L1109).
- **Corroborating evidence:** `grep -rl ExpireWorkItem src/Hexalith.Works/` → cascade files only; [`WorksHost.cs:80–99`](../../../src/Hexalith.Works/Runtime/WorksHost.cs#L80) (date-resume reminders only); [`sprint-status.yaml`](../../implementation-artifacts/sprint-status.yaml); [`epics.md:371, 405`](../epics.md#L371); [`prd.md:221–222, 415`](../prds/prd-works-2026-06-14/prd.md#L221).
- **Found by:** Conformance CONF-01, CONF-08, CONF-09; rubric walker RW-02.
- **Disposition:** **Discuss.** Route an "expiry reminder adapter + platform TTL options" story and the registry/fan-out stories through sprint planning; correct R6's "today in Works" to "target"; add open-register rows for AD-21 interim exposure and AD-22 until the stories exist. No change applied.

### VAL2-H04 — Expiry cannot reuse the AD-11 index or name function as written, and a stale expiry reminder expires a live item

- **Issue:** AD-25 says missed expiries reconcile "through the same indexed pending-source protocol as `DateReached`". The pending-await index has no `kind`; the reconciler dispatches every past-due entry as a `ResumeWorkItem`; the reminder name is bound to an await-condition key and `DueDate` is a `DateOnly` whose firing instant depends on platform TZ/TTL options. Separately, cancellation of the old reminder on reschedule is best-effort and a new instant creates a *second* reminder rather than replacing the first; `ExpireWorkItem` carries no due-instant witness, so the clock-free `Handle` cannot refuse a stale fire.
- **Divergence/risk:** A missed expiry is *resumed* (and rejected) instead of expired; two units key expiry differently or build a second index; an item rescheduled from October to December is expired in October by the surviving first reminder and the terminal cascade cancels its subtree — an ordinary at-least-once race, no forgery needed, and the same hole admits a replayed callback.
- **Source:** [`architecture.md:397–412`](../architecture.md#L397) (AD-25 Identity/Firing/Recovery), [`:760–762`](../architecture.md#L760) (name inputs), [`:137–157`](../architecture.md#L137) (AD-11/AD-12).
- **Corroborating evidence:** [`PendingDateAwait.cs:10–14`](../../../src/Hexalith.Works/Reminders/PendingDateAwait.cs#L10) (no kind); [`PendingDateAwaitProjection.cs:25–82`](../../../src/Hexalith.Works/Reminders/PendingDateAwaitProjection.cs#L25); [`DateReminderReconciler.cs:96–137`](../../../src/Hexalith.Works/Reminders/DateReminderReconciler.cs#L96) (past-due ⇒ resume, re-arms stale entries); [`DateReminderName.cs:25–54`](../../../src/Hexalith.Works/Reminders/DateReminderName.cs#L25); [`DateReminderActor.cs:79–90`](../../../src/Hexalith.Works/Reminders/DateReminderActor.cs#L79) (unregister swallows failures); [`WorkItemSchedule.cs:3`](../../../src/Hexalith.Works.Contracts/ValueObjects/WorkItemSchedule.cs#L3); [`lifecycle-transition-matrix.md:250–260`](../../../docs/lifecycle-transition-matrix.md#L250).
- **Found by:** Adversarial ADV-07; security SEC-03.
- **Disposition:** **Autofix in Update.** Pending entries carry `Kind ∈ {DateResume, Expiry, ReservationTimeout}` and a stored `Key`; the reconciler dispatches by kind; expiry names embed the computed UTC firing instant recorded in the index so cancel uses the stored key; `ExpireWorkItem` carries `DueInstant` and `Handle` rejects a mismatch against state (pure, no clock); add "reschedule then stale fire" to R6 acceptance. No change applied.

### VAL2-H05 — Two reminder registrars can be built on two actors, and AD-24 then denies one of them

- **Issue:** AD-11 says an item "registers a self-targeted durable actor reminder" without naming the actor type or host process; R6 moves the reminder seam into the SDK. Today reminders live on a dedicated `DateReminderActor` hosted in the Works process with a hashed actor id; an SDK implementer reading "self-targeted" registers on the EventStore aggregate actor in the EventStore process. AD-24 then requires callbacks and register/cancel only from "the Works actor identity".
- **Divergence/risk:** Two reminder registries for one item; reconciliation re-registers on one actor and cannot see the other (double firing or orphans); the SDK's registrations originate from the `eventstore` app id and are denied by deny-by-default, or hidden by the sandbox exemption until production; AD-24's positive-path test passes on whichever actor its author picked.
- **Source:** [`architecture.md:137–149`](../architecture.md#L137), [`:265`](../architecture.md#L265), [`:377–383`](../architecture.md#L377), [`:697`](../architecture.md#L697).
- **Corroborating evidence:** [`DateReminderActor.cs:17, 34–48`](../../../src/Hexalith.Works/Reminders/DateReminderActor.cs#L17); [`DaprDateReminderScheduler.cs:27–28`](../../../src/Hexalith.Works/Reminders/DaprDateReminderScheduler.cs#L27); [`WorksRecoveryExtensions.cs:93–98`](../../../src/Hexalith.Works/Runtime/WorksRecoveryExtensions.cs#L93); [`accesscontrol.works.yaml`](../../../src/Hexalith.Works.AppHost/DaprComponents/accesscontrol.works.yaml) (no actor-route entry).
- **Found by:** Adversarial ADV-06.
- **Disposition:** **Autofix in Update.** One actor type (`WorksReminderActor`) hosted in the Works service for all reminder kinds, actor id derived from (tenant, aggregate), never the EventStore aggregate actor; the R6 seam is a library the Works host composes and does not host actors; AD-24's "Works actor identity" means this actor's app id. No change applied.

### VAL2-H06 — The registry aggregate's identity, routing, and runtime seams are unowned

- **Issue:** AD-21 never fixes the registry's `{tenant}:{domain}:{aggregateId}`, topic, or subscription route; the R3 processor's identity contract requires domain `work`, payload `TenantId` + `WorkItemId`, and `AggregateId == WorkItemId`, so every `EdgeReserved` is dropped or unrouted. The AD-21 protocol also needs (i) reserve→spawn and rejection→release dispatch (R7 lists only cascade and child-completion), (ii) a "platform-configured reservation timeout" nobody fires, and (iii) someone to stamp `MaxDepth`/tenant policy onto the reserve command while "the kernel never reads configuration" and callers may not supply limits. AD-20's own Prevents names exactly this double-abandonment failure.
- **Divergence/risk:** The reactor is starved of registry events so no attachment ever completes, or a registry actor id collides with a legal `WorkItemId`; the Works registry author assumes the SDK runner dispatches its translations and fires its timeout while the SDK maintainer builds R6/R7 to the row text; a caller-supplied `MaxDepth` slips back in, recreating the VAL-C02 hole.
- **Source:** [`architecture.md:246–249`](../architecture.md#L246), [`:257–270`](../architecture.md#L257) (matrix), [`:272–299`](../architecture.md#L272), [`:313`](../architecture.md#L313), [`:201–202`](../architecture.md#L201), [`:448`](../architecture.md#L448) (VAL-H09).
- **Corroborating evidence:** [`WorksDomainEventProcessor.cs:40–44, 122–138, 186–197`](../../../src/Hexalith.Works/Runtime/Events/WorksDomainEventProcessor.cs#L40); [`WorksEventIdentity.cs:11–38`](../../../src/Hexalith.Works/Runtime/WorksEventIdentity.cs#L11); [`Program.cs:92–99`](../../../src/Hexalith.Works.AppHost/Program.cs#L92) (domain `work` routing only); [`AggregateIdentity.cs:92–94`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore.Contracts/Identity/AggregateIdentity.cs#L92); [`SpawnChild.cs:11–15, 29–32`](../../../src/Hexalith.Works.Contracts/Commands/SpawnChild.cs#L11) (caller-fed `MaxDepth = 32`).
- **Found by:** Adversarial ADV-04, ADV-10; rubric walker RW-04.
- **Disposition:** **Autofix in Update.** Fix the registry identity (recommended: domain `work-tree`, aggregate id = tenant id, topic `work-tree.events`, route `/work-tree/events`, per-aggregate-type identity contract in R3, platform registration as an R1 obligation); extend R7 to registry translations; add a row for the reservation-timeout timer; name the policy-enrichment point (recommended: the R11 submission seam stamps platform `MaxDepth`/timeout as audited non-caller fields). Add both rows to the VAL-H09 table. No change applied.

### VAL2-H07 — Deterministic MessageId derivation already has three incompatible styles; VAL-H10 is open while AD-21 depends on it

- **Issue:** AD-02 declines transport idempotency; R11 names the seam; AD-21 conditions the registry story on VAL-H10 without stating the derivation function. Works already has three schemes: raw concatenation for cascade and child-completion (`{kind}-{tenant}-{parent}-{seq}-{child}`), a SHA-256 name for date resume, and `MessageId = CausationId` in the submitter. Raw-concatenation ids embed aggregate ids that legally contain `.` and `_`, which the gateway `MessageId` regex rejects, and can exceed its 128-character cap; the SDK offers a second idempotency carrier (`IdempotencyKey` / `IIdempotencyIntentAdapter`) Works ignores.
- **Divergence/risk:** The gateway rejects mechanical commands for a subset of legal ids (a silent recovery gap today); duplicate suppression differs by originator; the registry saga has no defined replay result for a re-submitted reserve.
- **Source:** [`architecture.md:49–55`](../architecture.md#L49), [`:270`](../architecture.md#L270), [`:313–316`](../architecture.md#L313), [`:449`](../architecture.md#L449).
- **Corroborating evidence:** [`CascadeCommands.cs:18–19`](../../../src/Hexalith.Works/Recovery/Cascade/CascadeCommands.cs#L18); [`ChildCompletionResume.cs:20`](../../../src/Hexalith.Works/Recovery/ChildCompletion/ChildCompletionResume.cs#L20); [`DateResume.cs:33`](../../../src/Hexalith.Works/Reminders/DateResume.cs#L33); [`EventStoreGatewayWorkCommandSubmitter.cs:23–30`](../../../src/Hexalith.Works/Runtime/EventStoreGatewayWorkCommandSubmitter.cs#L23); [`SubmitCommandRequestValidator.cs:23–41, 93–97`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore/Validation/SubmitCommandRequestValidator.cs#L23); [`AggregateIdentity.cs:11`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore.Contracts/Identity/AggregateIdentity.cs#L11); [`IIdempotencyIntentAdapter.cs:6–25`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore.DomainService/IIdempotencyIntentAdapter.cs#L6).
- **Found by:** Adversarial ADV-05.
- **Disposition:** **Autofix in Update** — bind VAL-H10 now as a new AD: one pure derivation function in the Reactor (`{originator}-{sha256hex32(tenant ␟ aggregate ␟ causal type ␟ causal sequence ␟ intent)}`), `CausationId` = causal envelope `MessageId`, `IdempotencyKey` unused, replay of an admitted id returns the original result; retention tier bound in R11. Must precede the registry story. No change applied.

### VAL2-H08 — AD-23's delegation context has no shape; the as-built internal path is a global-admin super-identity

- **Issue:** AD-23 requires internal originators to act under "an authenticated workload identity plus an explicit, auditable tenant-delegation context". Nothing says what the context is, who mints it, what it scopes, or where it is persisted. As built, callers admitted through `DaprInternal` are minted `global_admin=true` and the tenant validator allows any tenant; in the Keycloak sandbox the Works service is given the tenant-A *user's* client credentials — the borrowed user identity AD-23 forbids. Per-command origin restriction (AD-24's `SpawnChild` rule) has no named enforcement point.
- **Divergence/risk:** A platform implementer and the Works submitter cannot build the delegation compatibly from the text; a reminder firing, cascade replay, or compromised recovery loop can submit any command to any tenant with no delegation evidence, and every date-resume for every tenant is persisted as one user in the sandbox.
- **Source:** [`architecture.md:356–362`](../architecture.md#L356), [`:377–395`](../architecture.md#L377), [`:678`](../architecture.md#L678).
- **Corroborating evidence:** [`DaprInternalAuthenticationHandler.cs:21–38`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore/Authentication/DaprInternalAuthenticationHandler.cs#L21); [`ClaimsTenantValidator.cs:23–24`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore/Authorization/ClaimsTenantValidator.cs#L23); [`CommandsController.cs:111–122`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore/Controllers/CommandsController.cs#L111); [`Program.cs:108, 224–242`](../../../src/Hexalith.Works.AppHost/Program.cs#L108); [`EventStoreGatewayWorkCommandSubmitter.cs:23–30`](../../../src/Hexalith.Works/Runtime/EventStoreGatewayWorkCommandSubmitter.cs#L23); [`DualPrincipalClaimsHelper.cs:14–17`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore/Authorization/DualPrincipalClaimsHelper.cs#L14) (an RFC 8693 `act` helper already exists on the query path); [`AuthorizationBehavior.cs:80–96`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore/Pipeline/AuthorizationBehavior.cs#L80) (`IRbacValidator` seam, unnamed by any AD).
- **Found by:** Security SEC-01, SEC-05; adversarial ADV-14.
- **Disposition:** **Discuss** (before the registry and expiry stories, which add internal originators). Minimum binding: a per-submission tenant-scoped assertion `{workloadId, tenantId, causationId, purpose}` on the R11 contract, persisted on the envelope; internal workload identities are never global administrators; the sandbox never uses user client-credentials for the Works service; gateway RBAC keyed by (workload identity, command type) for `SpawnChild`, replay, and rebuild; a negative test for out-of-delegation submission. No change applied.

### VAL2-H09 — AD-24's exclusive-originator rules cannot be expressed in Dapr access control; the app channel is unprotected

- **Issue:** Dapr access-control policy matches app id, trust domain, namespace, and service-invocation operations only. It does not govern pub/sub deliveries the local sidecar pushes to `/work/events`, actor reminder callbacks pushed to the actor routes, anything reaching the app port directly, or which command type a caller may submit. AD-24 does not say which mechanism carries each rule. The Dapr app-channel control for this (`dapr-api-token` validation) exists nowhere in Works or the DomainService SDK; the subscription route intentionally carries no caller authentication; two sandbox access-control configs still default to allow; the "dev-only exemption" is YAML comments rather than a control.
- **Divergence/risk:** Any process reaching the Works app port can post a self-consistent envelope to trigger cascade cancel, resume, or reminder registration, or invoke an actor `remind` early; the processor's identity checks only prove attacker-controlled fields agree. R3/R6/R10/R11 implementers will pick different enforcement points.
- **Source:** [`architecture.md:372–384`](../architecture.md#L372), [`:262`](../architecture.md#L262), [`:269`](../architecture.md#L269), [`:392–395`](../architecture.md#L392).
- **Corroborating evidence:** [`accesscontrol.works.yaml:19–50`](../../../src/Hexalith.Works.AppHost/DaprComponents/accesscontrol.works.yaml#L19); [`WorksDomainEventEndpointExtensions.cs:16–29, 70–82`](../../../src/Hexalith.Works/Runtime/Events/WorksDomainEventEndpointExtensions.cs#L16); [`WorksDomainEventProcessor.cs:132–152`](../../../src/Hexalith.Works/Runtime/Events/WorksDomainEventProcessor.cs#L132); [`DateReminderActor.cs:52–77`](../../../src/Hexalith.Works/Reminders/DateReminderActor.cs#L52); [`DaprAppChannelTokenMiddleware.cs:11–13`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore.Operations/Security/DaprAppChannelTokenMiddleware.cs#L11) (the only app-channel guard in the ecosystem, in Operations only); [Dapr invoke allowlist](https://docs.dapr.io/operations/configuration/invoke-allowlist/); [Dapr app API token](https://docs.dapr.io/operations/security/app-api-token/).
- **Found by:** Security SEC-02, SEC-06.
- **Disposition:** **Discuss.** Amend AD-24 to name the enforcement point per rule: sidecar ACL for service-invocation routes; broker ACLs plus publishing scopes for topic origin; app-channel token validated by the DomainService SDK on every route plus network policy admitting only the local sidecar; gateway RBAC for per-command origin. Add the token middleware to the SDK (generic capability lands in the SDK first, per AD-20). Extend the 4.9 negative suite with direct `/work/events` and direct actor `remind` calls. No change applied.

### VAL2-H10 — VAL-H12's revisit condition is too late for an immutable, additive-only store

- **Issue:** The only privacy rules are "never log payloads" and the open VAL-H12 row revisiting "before production data is admitted". AD-01 forbids `V2` types and requires every event ever produced to stay deserializable. Erasure in such a store is only possible by crypto-shredding applied at persistence, an EventStore-envelope capability. Every durable type shipped before that decision bakes plaintext free text into a shape that cannot be migrated.
- **Divergence/risk:** `Obligation.Description`, `ProgressReported.Note`, executor references, and the envelope `UserId` (the OIDC `sub`) are persisted verbatim in streams, rebuild candidates, read models, and dead-letter queues whose retention is manual. On tenant offboarding or an erasure request the only options are a hard stream delete that breaks AD-01/AD-16 or non-compliance. Reminder names and read-model keys are already PII-free, which is the good news.
- **Source:** [`architecture.md:451`](../architecture.md#L451), [`:478`](../architecture.md#L478), [`:494`](../architecture.md#L494), [`:782–786`](../architecture.md#L782), [`:847–848`](../architecture.md#L847).
- **Corroborating evidence:** [`Obligation.cs`](../../../src/Hexalith.Works.Contracts/ValueObjects/Obligation.cs); [`ProgressReported.cs:15`](../../../src/Hexalith.Works.Contracts/Events/ProgressReported.cs#L15); [`WorkItemCreated.v1.json`](../../../tests/Hexalith.Works.IntegrationTests/SchemaEvolution/EventPersisterGolden/WorkItemCreated.v1.json) (golden corpus, plaintext); [`EventPersister.cs:104–121`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore.Server/Events/EventPersister.cs#L104); [`subscriber-dead-letter-operator.md:62–76`](../../../docs/operations/subscriber-dead-letter-operator.md#L62).
- **Found by:** Security SEC-04.
- **Disposition:** **Discuss** (owner: Platform Maintainer with EventStore). Tighten the revisit condition to "before the next durable-type catalog addition ships (AD-21 registry types, AD-25 `ExpireWorkItem`) and before non-synthetic data enters any shared environment including staging"; record field classification for the existing 40 types, the field-level-encryption vs opaque-reference choice, DLQ/candidate/read-model retention, and that envelope `UserId` is a pseudonymous subject id. No change applied.

### VAL2-H11 — VAL-H11's revisit condition has already lapsed

- **Issue:** VAL-H11 (schema compatibility matrix) revisits "before the Story 1.5 catalog change ships". Story 1.5 is done and the catalog is at 40 serialized types. AD-21 and AD-25 add further additive types; AD-01 mandates tolerant evolution with no reader/writer-direction or unknown-enum rule defined anywhere.
- **Divergence/risk:** The registry story and the SDK consumers in another repository can pick incompatible tolerance rules (unknown enum → reject vs default; missing additive field → null vs required) with no matrix to arbitrate; the condition meant to catch this is in the past.
- **Source:** [`architecture.md:450`](../architecture.md#L450), [`:38–47`](../architecture.md#L38), [`:283–284`](../architecture.md#L283).
- **Corroborating evidence:** [`sprint-status.yaml:50`](../../implementation-artifacts/sprint-status.yaml#L50); [`addendum.md:30`](../prds/prd-works-2026-06-14/addendum.md#L30); `EventShapeGovernanceTests.cs` governs shape, not compatibility direction.
- **Found by:** Rubric walker RW-03.
- **Disposition:** **Autofix in Update** — re-pin the condition to "before the AD-21 registry contracts enter the catalog (same gate as VAL-H10)". **Discuss** ownership (catalog governance in Works, readers in the SDK). No change applied.

### VAL2-H12 — Works absorbed platform-owned R1/R10 topology after AD-20 bound it to Hexalith.Platform; the register is silent

- **Issue:** Commit `33a27e2` (2026-09-06, the same day the register was bound) added a Works-owned Sentry container, AppHost-owned TLS placement and scheduler containers pinned to `daprio/dapr:1.18.3`, mTLS blocks in every access-control config, and readiness plumbing; `42c4318` (2026-09-08) added OIDC client parameters and development symmetric-key JWT validation in the Works AppHost. AD-20 R1/R10 name Hexalith.Platform as owner; AD-24 places the sandbox exemption in the platform repository; AD-19 makes the Dapr runtime pin platform-owned. The scope expansion was approved inside Story 4.8's review trail only. The register also states "current 13.4.6"/"13.5.3" undated, breaking its own AD-19.
- **Divergence/risk:** The migration surface AD-20 exists to shrink grew by roughly a thousand lines the day it was bound; the R10 proof now lives in the Works lane rather than `verify-works-host`; a Dapr runtime version is hard-coded three times in Works code; the double-abandonment risk AD-20 names rises because more behaviour must be re-proven in the platform lane before removal.
- **Source:** [`architecture.md:226–253`](../architecture.md#L226) (AD-20), [`:260`](../architecture.md#L260), [`:269`](../architecture.md#L269), [`:216–224`](../architecture.md#L216) (AD-19), [`:392–395`](../architecture.md#L392).
- **Corroborating evidence:** `git show 33a27e2 --stat`; [`DaprSelfHostedMtls.cs:36, 80, 90, 111`](../../../src/Hexalith.Works.AppHost/DaprSelfHostedMtls.cs#L36); [`accesscontrol.works.yaml:8–17`](../../../src/Hexalith.Works.AppHost/DaprComponents/accesscontrol.works.yaml#L8); [`4-8-register-and-reconcile-date-reminders-durably.md:370`](../../implementation-artifacts/4-8-register-and-reconcile-date-reminders-durably.md#L370) ("after the human approved the Sentry/mTLS scope expansion"); [`sprint-change-proposal-2026-09-06.md`](../sprint-change-proposal-2026-09-06.md) (no mention).
- **Found by:** Conformance CONF-03; technology TECH-04; rubric walker RW-11.
- **Disposition:** **Discuss, then autofix.** Add an open-register row "R1/R10 debt: Works-owned mTLS control plane and JWT composition (2026-09-06/08) must be reproduced in Hexalith.Platform before removal"; move the runtime literal to platform-owned configuration; date the AD-20 version statements; adopt a rule that host-edge topology changes after 2026-09-06 are logged against a matrix row. No change applied.

### VAL2-H13 — AD-20's "single 13.5.x family" rests on a preview-only Dapr integration and an empty platform host; three target seams do not exist yet

- **Issue:** The only Aspire→Dapr integration the ecosystem uses is `CommunityToolkit.Aspire.Hosting.Dapr`, stable at 13.0.0; only a `13.5.0-preview` tracks Aspire 13.5, and that is what Works pins — the register never names the toolkit or its preview status. The Hexalith.Platform checkout is a nine-line single-file `apphost.cs` with an empty builder (no Dapr, Sentry, placement, scheduler, Works, or EventStore composition), on SDK 10.0.302 against Works' 10.0.400. The R4 fan-out seam, R6 durable-reminder seam, and R7 process-runner seam have zero presence in EventStore v3.103.0; only R11's gateway client exists, and the matrix never states absence per row or cites an EventStore backlog item. R4 also names only the legacy `/project` route while EventStore already ships a fenced `/project/v2` delivery seam Works' access control does not allow.
- **Divergence/risk:** A reader of AD-20 alone assumes a near-ready host on a stable graph; Story 4.9's R1 must re-create everything the Works AppHost does today inside a repository that has none of it; R4/R6/R7 acceptance is gated on another repository shipping three features with no referenced story or version target, and AD-22's Rule cannot be met until R4 exists.
- **Source:** [`architecture.md:234–246`](../architecture.md#L234), [`:260–270`](../architecture.md#L260), [`:336–337`](../architecture.md#L336), [`:184–194`](../architecture.md#L184) (AD-16).
- **Corroborating evidence:** [`global.json`](../../../global.json); [`Directory.Packages.props`](../../../references/Hexalith.Builds/Props/Directory.Packages.props) (`Aspire.Hosting 13.5.3`, toolkit `13.5.0-preview.1.260825-0345`); `~/projects/hexalith/platform/apphost.cs:1` (`#:sdk Aspire.AppHost.Sdk@13.4.6`, empty builder) and its `global.json` (10.0.302); EventStore grep for `FanOut`, `IDurableReminder*`, `ProcessRunner` → no matches; [`IEventStoreGatewayClient.cs`](../../../references/Hexalith.EventStore/src/Hexalith.EventStore.Client/Gateway/IEventStoreGatewayClient.cs); [CommunityToolkit.Aspire.Hosting.Dapr on NuGet](https://www.nuget.org/packages/CommunityToolkit.Aspire.Hosting.Dapr); [Aspire.Hosting on NuGet](https://www.nuget.org/packages/Aspire.Hosting).
- **Found by:** Technology TECH-02, TECH-03, TECH-07, TECH-10.
- **Disposition:** **Discuss.** Amend AD-20 to name the toolkit and its preview status with an upgrade-on-stable trigger; state plainly that the platform host is an empty scaffold as of 2026-09-08; fold the SDK band into "move together"; add an "exists in EventStore today?" column to the matrix (R11 yes; R4/R6/R7 absent, with a backlog reference); have AD-16/R4 acknowledge `/project/v2` and its fencing token. No change applied.

## Medium tail

Thirteen deduplicated medium findings. Full text lives in the linked lens reviews.

| ID | Finding | Source | Lens IDs | Disposition |
| --- | --- | --- | --- | --- |
| VAL2-M01 | The spine has not absorbed the 2026-09-08 PRD amendment (FR-26 new; FR-2/7/8/11/12/15/20 changed; §9 NFR; provisional performance numbers); it still says "25 FRs", and the addendum lists edits H1/H2/H8/H12 owed to this document. FR-12 Unit inheritance at spawn and the FR-20 two-view predicate have no AD. | [`:459`](../architecture.md#L459), [`:1064`](../architecture.md#L1064), [`:1075–1077`](../architecture.md#L1075); [`addendum.md:60–72`](../prds/prd-works-2026-06-14/addendum.md#L60) | RW-06, CONF-07 | Discuss; then autofix the owed edits and add a register row naming the amended PRD as the spec baseline. |
| VAL2-M02 | AD-22 says fan-out writers "follow the fence" while AD-16 says no fence protocol exists; fan-out writes are not on the path a delivery quiesce can stop. Behaviour during an unbound fence is undefined. | [`:184–194`](../architecture.md#L184), [`:333–336`](../architecture.md#L333), [`:447`](../architecture.md#L447) | RW-07, ADV-12, TECH-03 | Discuss with Platform; interim text: fan-out writes are held back while a shared rebuild is staged. |
| VAL2-M03 | Duplicate or redelivered `SpawnChild` against a non-live parent produces the dedicated rejection, and the reactor releases a healthy `Attached` edge (the ADV-U2 no-op covers only identical live pairs). | [`:293–302`](../architecture.md#L293) | ADV-09 | Autofix: release only from `Reserved`, never from `Attached`. |
| VAL2-M04 | Reminder reconciliation posture is un-registered: commit `0527d12` lets a projection-side "parked candidate" veto the authoritative stream read; the startup pass gives up after roughly five seconds; recovery exhaustion is logged and swallowed while the host stays Ready. No AD Rule forbids the swallow (SEC-DI-05 residue). | [`:137–150`](../architecture.md#L137), [`:267`](../architecture.md#L267) (R8), [`:444`](../architecture.md#L444) | CONF-06, CONF-15, SEC-08 | Discuss before Story 4.8 is accepted: bind "exhaustion ⇒ durable evidence + degraded readiness" at R8 and give parked aggregates an explicit disposition. |
| VAL2-M05 | Two reserved control-plane tenant ids coexist (`tenants` in Works, `system` in EventStore); global recovery registries and the four-app shared actor store are unlisted (VAL-H09 / SEC-DI-06). | [`:174–183`](../architecture.md#L174), [`:448`](../architecture.md#L448), [`:984–986`](../architecture.md#L984) | ADV-13, SEC-12 | Autofix an interim rule naming one reserved id; defer the table to VAL-H09. |
| VAL2-M06 | SEC-DI-07 (uniform quarantine contract) from the 2026-09-05 gate was dropped without disposition — neither adopted nor in the open register. | [`:436–453`](../architecture.md#L436) | SEC-09 | Autofix: add the row. |
| VAL2-M07 | "Audited" is asserted five times with no audit record shape or sink; AD-21 operator repair has no command or event. | [`:303–307`](../architecture.md#L303), [`:360–362`](../architecture.md#L360) | SEC-10 | Discuss. |
| VAL2-M08 | Silent dimensions: secrets management (broker, Sentry, OIDC, `DAPR_API_TOKEN` ownership), DR/backup of the event store and multi-region, environment definitions, API-surface versioning, supply chain (the Works root sets `NuGetAudit=false`, overriding the Builds baseline; no CI definition in the repo). The existing dead-letter runbook is unreferenced. | [`:695–703`](../architecture.md#L695) | RW-09, SEC-11 | Autofix a "decided / deferred / open" line per dimension; defer the rest with owners. |
| VAL2-M09 | Load-bearing rules live only in prose declared "seed": FR-17 zero-branching on executor kind, FR-21/22 reference-not-copy and ports, the NFR-6 observability/privacy floor. | [`:26–36`](../architecture.md#L26), [`:815–821`](../architecture.md#L815) | RW-08, CONF-14 | Discuss: promote to ADs or mark those subsections binding. |
| VAL2-M10 | The VAL-M01 row is stale (the architecture-test lane builds today; its revisit names a story that does not exist) and the fitness lane ratifies the Works-owned AppHost, inverting Story 4.9 AC6. | [`:452`](../architecture.md#L452), [`:1035–1036`](../architecture.md#L1035) | RW-10, CONF-04 | Autofix the row wording; defer the flip to Story 4.9. |
| VAL2-M11 | Reminder durability is reality-checked only against a single-replica self-hosted Scheduler; HA/backup is unbound; the Dapr .NET SDK 1.19 preview is a from-scratch actor rewrite the classic `DateReminderActor` does not track. | [`:137–149`](../architecture.md#L137), [`:265`](../architecture.md#L265) | TECH-01 | Defer to R6 with an explicit horizon note. |
| VAL2-M12 | A forged or replayed descendant contribution at the `/project` seam poisons ancestor LWW slots; AD-22 amplifies it; the "authenticated provenance before `SequenceNumber` is trusted" check has no bound location. | [`:318–342`](../architecture.md#L318), [`:384`](../architecture.md#L384) | SEC-07 | Autofix: name the seam (SDK delivery pipeline) that verifies provenance before any Works strategy runs. |
| VAL2-M13 | AD-21 is unbuilt and the interim state is the hole it prevents; Stories 3.1/3.2 and `docs/work-tree-shape-guard.md` still describe caller-fed authority with no supersession note. | [`:272–316`](../architecture.md#L272), [`:1104–1105`](../architecture.md#L1104) | CONF-08 | Autofix supersession notes; add an "interim exposure" register row with mitigation and revisit. |

## Low tail

Eight deduplicated lows: AD-05 names a `Meter` type while the code type is `WorkItemEffort` (CONF-10); AD-12's Rule is a revisit instruction rather than a falsifiable rule (RW-12); AD-18's Binds mention the executable and Testing while the allowlist covers four projects (RW-14); AD-03 ordinal order on case-preserved ids across originators does not approximate creation order (ADV-15); "production ingress is prohibited until live" has no enforcing mechanism (SEC-13); frontmatter still says `status: 'complete'` from 2026-06-14 with dangling ADV-U*/SEC-DI-*/EXT-HOST-1 references and a structure tree listing folders that do not exist (RW-13, CONF-12); epics Additional Requirements carry pre-register text (CONF-13); test projects carry a mixed VSTest/MTP package set, Fluent UI is a prerelease pin, and `Hexalith.EventStore.Operations` has no central package pin (TECH-05, TECH-06, TECH-11). Dapr semantic claims (at-least-once, turn lock, ETag, Scheduler default, mTLS trust domain, deny-by-default) and sibling-module seams were all verified correct (TECH-08, TECH-09).

## Good-spine checklist scorecard

| Dimension | 2026-09-05 | 2026-09-08 | Assessment |
| --- | --- | --- | --- |
| Real divergence points fixed | Fail | **Concern** | Host, trust, tree authority, live roll-up, and expiry now have owners; the protocol details inside AD-06/21/22/25 still let two units diverge (C01–C03, H04–H07). |
| Rules enforceable and preventive | Fail | **Pass (minor)** | 22 of 25 Rules are falsifiable and code-backed; AD-13 is wrong (H01), AD-12 is a revisit instruction, AD-19 is violated by AD-20's own text. |
| Deferred items safe | Concern | **Fail** | VAL-H11's condition has lapsed (H11); VAL-H06–H09 and AD-23/24 acceptance hang on Story 4.9 rows the story does not carry (H02); VAL-H12 revisits too late (H10). |
| Named technology current | Fail | **Pass** | Every pin verified current on the web; Dapr runtime vs SDK now distinct. Remaining concern is disclosure, not staleness (H13). |
| Brownfield ratification | Fail | **Pass (with drift)** | 15 ADs ratified, 6 honestly marked as target state; drift on AD-05 naming, AD-06 storage shape, AD-13 rule; post-06 topology work outran AD-20 (H12). |
| Spec capability coverage | Concern | **Concern** | Every FR-1…25 has a home; the 2026-09-08 PRD amendment is unabsorbed (M01); AD-21/22/25 have no stories (H03). |
| Inherited constraints honored | Fail | **Pass** | Domain/platform boundary now executable: named destination, owner, seam matrix. |
| Structural dimensions complete | Concern | **Concern** | Domain, data, test, and identity dimensions decided or deferred; secrets, DR/backup, environments, API versioning, supply chain still silent (M08). |

## Prior-gate carry-over

| 2026-09-05 item | Status at this gate |
| --- | --- |
| VAL-C01 host ownership | Closed by AD-20. Residue: H12 (Works absorbed R1/R10), H13 (preview toolkit, empty host, absent seams). |
| VAL-C02 tree authority | Closed at invariant level by AD-21. Residue: C03 (creation act, witness, public `Parent`), H06 (identity/routing/seams), M03, M13. |
| VAL-C03 live roll-up | Closed at design level by AD-22. Residue: C01 (document/merge), C02 (contribution), M02, M12. |
| VAL-C04 identity provenance | Closed at invariant level by AD-23. Residue: H08 (delegation shape), H02 (acceptance). |
| VAL-C05 trusted origin | Closed at invariant level by AD-24. Residue: H09 (enforcement points), H05 (which actor). |
| VAL-H01 expiry | Closed at design level by AD-25. Residue: H03 (no code, no story), H04 (index/name reuse, stale fire). |
| VAL-H02, H03, H04, H05 | Closed (dependency graph, identity order via correct-course, AD register shape, version authority). |
| VAL-H06, H07, H08, H09 | Open in register; revisit conditions unsafe (H02). VAL-H09 also M05. |
| VAL-H10 | Open; must bind before the registry story — proposed as a new AD in H07. |
| VAL-H11 | Open; condition lapsed (H11). |
| VAL-H12 | Open; condition too late (H10). |
| VAL-M01 | Open; row stale (M10). VAL-M02, M03, M04 closed as wording fixes. |
| SEC-DI-07 quarantine contract | Dropped without disposition (M06). |

## Prioritized update plan

1. **Before the registry story is drafted (blocks AD-21/AD-22/AD-25 stories):** C01, C02, C03, H04, H05, H06, H07, H11, M03, M13. These are mostly autofixable register text once the two discuss items (C03 creation act; H07 as a new AD binding VAL-H10) are decided.
2. **Before Story 4.9 leaves backlog:** H02 (amend Story 4.9 ACs), H03 (route the expiry, registry, and fan-out stories), H12 (record the R1/R10 debt), H13 (disclose preview toolkit, empty host, absent seams), M10.
3. **Before the next durable-type catalog addition:** H10 (privacy revisit), H11 (compatibility matrix), M06.
4. **Platform Maintainer decisions:** H08 (delegation context shape), H09 (enforcement points and SDK token middleware), M02 (fence), M07 (audit shape), M08 (silent dimensions), M11.
5. **Hygiene sweep with the Update:** H01 (AD-13), M01 (absorb the 2026-09-08 PRD), M04, M05, M09, M12, and the low tail.

## Review artifacts

- [`reviews/review-rubric-walker.md`](reviews/review-rubric-walker.md) — good-spine checklist, 14 findings.
- [`reviews/review-adversarial-divergence.md`](reviews/review-adversarial-divergence.md) — two-unit divergence scenarios with proposed closing rules, 15 findings.
- [`reviews/review-technology-reality.md`](reviews/review-technology-reality.md) — 31-row claims table with web and repo truth, 11 findings.
- [`reviews/review-security-data-integrity.md`](reviews/review-security-data-integrity.md) — prior SEC-DI disposition and 13 findings.
- [`reviews/review-code-spec-conformance.md`](reviews/review-code-spec-conformance.md) — AD ratification table, post-06 drift table, FR/NFR/story coverage, 15 findings.
- [`lint-spine.json`](lint-spine.json) — deterministic lint output.
- Target: [`../architecture.md`](../architecture.md), unchanged. Prior run: [`../architecture/architecture-works-2026-09-06/.memlog.md`](../architecture/architecture-works-2026-09-06/.memlog.md).
