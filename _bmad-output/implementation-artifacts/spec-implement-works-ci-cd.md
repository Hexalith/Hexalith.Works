---
title: 'Implement Works CI/CD and NuGet release verification'
type: 'feature'
created: '2026-09-18'
status: 'in-progress'
route: 'dispatch'
review_loop_iteration: 2
baseline_commit: 'a9f4d0a3e9a29ef0b419a9b5b10f6f2c2aff2528'
context:
  - '{project-root}/references/Hexalith.AI.Tools/hexalith-llm-instructions.md'
  - '{project-root}/references/Hexalith.Builds/.github/workflows/ci-cd-standards.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Works has no CI/release workflows, authoritative package inventory, or semantic-release tooling; Release builds still consume sibling libraries from source, one architecture test has drifted from SDK `10.0.401`, and all five intended Works package IDs currently return 404 from NuGet.org. The broad integration baseline also exposed an intermittent AppHost restart race in which the mTLS scheduler stayed unhealthy, although the focused failing method then passed.

**Approach:** Apply the common Tenants/EventStore/FrontComposer operating model: thin shared-workflow callers, package-mode Release builds, manifest-driven packing and isolated consumers, and an operator-only fail-closed release with exact-source and post-publication checks. Repair the two observed test defects and verify locally without committing, pushing, changing GitHub settings/secrets, or dispatching a release.

## Boundaries & Constraints

**Always:** Use the root-tracked Builds SHA `04d961759994396132bb2b113ee465b64740a543` as the immutable CI/release contract; keep Debug sibling-source references and Release NuGet references; run every test project separately with Microsoft.Testing.Platform; keep release manual, non-cancelling, production-protected, exact-green-`main` gated, collision-failing, and manifest/count checked; publish exactly Contracts, Server, Projections, Reactor, and Testing under one version; keep NuGet audit visible.

**Never:** Do not auto-release on push, use `--skip-duplicate`, publish hosts/samples/test runners, copy module-specific UI/container/recovery machinery, expose secrets, modify submodules, or perform commit/push/release/repository-configuration side effects. A registry check reports absence honestly; it does not authorize publication.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|---------------|----------------------------|----------------|
| CI | Push or PR to `main` | Release package-mode restore/build, package/consumer checks, and four blocking test projects | Any shard or inventory drift fails the run and uploads test evidence |
| Invalid release | Non-main/stale SHA or no exact successful push CI | Protected release job is never entered | Fail before secrets/environment approval |
| Publication collision | Any target ID/version already exists | No NuGet write occurs | Fail both verify and publish preflights; never skip duplicates |
| Partial publication | Tag exists but one of five NuGets is absent | Release verification remains red | Name every missing ID/version |
| Current registry | Five Works IDs are unregistered | Record the five 404 results | Do not synthesize success or dispatch a release |

</frozen-after-approval>

## Code Map

- `references/Hexalith.Builds/.github/workflows/{domain-ci,domain-release}.yml` -- shared execution contracts; consume at the frozen SHA, do not edit.
- `references/Hexalith.{Tenants,EventStore,FrontComposer}/.github/workflows/` and their manifests/scripts -- proven patterns; reuse the common core only.
- `Directory.Build.props`, `Directory.Solution.props`, and external-reference `.csproj` files -- add unambiguous Debug/source versus Release/package selection and visible, non-blocking restore auditing. EventStore Operations may remain a source-path runtime resource, but must not pull its library closure into the Release compile graph.
- `src/Hexalith.Works.AppHost/HexalithEventStoreOperations.cs`, AppHost/integration project references, and direct Operations tests -- model the unpackageable Operations executable by project metadata/path rather than a Release compile dependency; remove Works-owned tests of EventStore internals or replace them with public topology/API assertions.
- `tests/Hexalith.Works.ArchitectureTests/FitnessTests/{BuildConfigurationTests,DependencyDirectionTests,CiCdConfigurationTests}.cs` -- repair the SDK assertion and enforce the complete package inventory, evaluated dependency modes, actual repository package-version origins, release controls, and executable negative-path script coverage.
- `tests/Hexalith.Works.IntegrationTests/WorksAppHostSmokeHarness.cs` -- separate the initial environmental prerequisite skip from hard restart-boundary failures, wait for Sentry/placement/scheduler health as well as application health, and preserve actionable teardown diagnostics.
- `.github/`, `.releaserc.json`, `package*.json`, `commitlint.config.mjs`, `scripts/`, `tools/release-packages.json`, and `xunit.runner.json` -- module-owned CI/release governance with a realistic build timeout, fail-on-skip execution, executable post-publication verification, final-version build/pack, exact dependency validation, and bounded same-run publication recovery.

## Tasks & Acceptance

**Execution:**
- [ ] `.github/workflows/{ci,commitlint,codeql,dependency-review,release}.yml`, `.github/dependabot.yml`, `xunit.runner.json`, and test-project content wiring -- retain immutable CI/security callers; give the combined build/test job at least 60 minutes; make every xUnit skip fail CI; and run post-publication verification after any started release without letting a later publication-freeze edit suppress it.
- [x] `.releaserc.json`, `package.json`, `package-lock.json`, `.nvmrc`, `commitlint.config.mjs` -- configure locked semantic-release/commitlint without changelog/git plugins or forbidden `chore` commits.
- [ ] `tools/release-packages.json`, `scripts/{pack-release-packages.py,push-release-packages.sh,validate-nuget-packages.py,validate-consumer-package-references.py,validate-release-secrets.sh,validate-publication-preflight.sh,verify-release-publication.sh}` -- define exactly five packages; compile with the resolved semantic version before packing; reject malformed dependency elements; validate nuspec ranges and the real `AssemblyInformationalVersion` by executing/reflection-loading isolated consumers rather than scanning DLL bytes; and remove any registry override that can separate preflight/verification from the publication destination.
- [ ] `scripts/push-release-packages.sh` and release recovery documentation -- freeze the ten local artifact hashes, retry bounded transient failures in the same invocation, reject a first-attempt 409 as a collision, and accept a 409 only after that invocation already attempted the unchanged artifact and received an ambiguous transport/server result. Do not claim cross-run recovery from an ephemeral runner; after retry exhaustion, report the partial version and require a new patch release.
- [ ] Script tests under `scripts/tests/` (or the narrowest repository-consistent equivalent) -- execute malformed manifest/package, missing dependency versions, mixed 404/200 collision, transient retry success, retry exhaustion, first-attempt 409 rejection, same-run ambiguous-result 409 recovery, and post-publication tag/package branches (matching, unrelated, unresolvable, zero, multiple, missing package, and transport error) rather than relying on source-text assertions alone.
- [ ] `Directory.Build.props`, `Directory.Solution.props`, `src/Hexalith.Works.Contracts/Hexalith.Works.Contracts.csproj`, `src/Hexalith.Works/Hexalith.Works.csproj`, and Architecture facts -- make local Debug source-based and CI Release package-based for published external libraries; reject non-boolean final as well as caller-supplied mode values; execute symmetric invalid-`UseNuGetDeps` and configuration-free fallback tests; enable all-mode audit with advisory warnings visible but non-blocking.
- [x] `src/Hexalith.Works.AppHost/{Hexalith.Works.AppHost.csproj,HexalithEventStoreOperations.cs}`, `tests/Hexalith.Works.IntegrationTests/Hexalith.Works.IntegrationTests.csproj`, and Operations-facing tests -- retain the executable Operations topology through project metadata/path without a compile-time source project reference or transitive source-library mix in Release.
- [ ] `tests/Hexalith.Works.ArchitectureTests/FitnessTests/{BuildConfigurationTests,DependencyDirectionTests,CiCdConfigurationTests}.cs` -- repair the SDK assertion and pin workflow, repository-wide package inventory/version ownership, dependency modes, fail-on-skip/timeout controls, executable post-publication branches, and no-auto-release invariants.
- [ ] `WorksAppHostSmokeHarness.cs` -- allow environmental skipping only in the explicit prerequisite probe; test port availability by exclusive bind rather than connect; make every start/restart boundary fail on lingering resources; wait for all three mTLS control-plane resources plus EventStore and Works health; preserve the original body exception across disposal/diagnostic failures; and fail a previously successful body when cleanup does not settle.
- [ ] `README.md` -- document local Debug verification, CI Release behavior, manual release prerequisites, bounded same-run retry and honest retry-exhaustion recovery, exact package inventory, and the currently unpublished status without overstating audit enforcement.

**Acceptance Criteria:**
- Given a clean checkout, when the mandatory local Debug check and CI-equivalent Release check run, then the Debug sibling-source graph compiles, the Release compile graph uses NuGet for published external libraries, the source-only Operations executable does not introduce a transitive compile-time library mix, the CI timeout accommodates the evidenced live-suite duration, and any skipped fact fails the gate.
- Given semantic-release supplies a version, when release packages are built and validated, then exactly the five manifest `.nupkg`/`.snupkg` pairs exist, every packaged assembly reports that version through `AssemblyInformationalVersion`, malformed or drifting nuspec dependency ranges fail, and isolated PackageReference consumers compile and execute.
- Given each test project is invoked separately, when the full gate runs, then Unit, Architecture, Property, and Integration suites pass without skips, including the focused reminder restart method and healthy Sentry, placement, and scheduler resources at every start.
- Given workflow/static and executable script validation runs, when callers and semantic-release hooks are inspected, then actionlint and negative-path release tests pass and release requires exact green `main`, protected production, explicit `NUGET_API_KEY`, immutable Builds identity, collision checks, checksum-locked bounded same-run retries without `--skip-duplicate`, and five-package post-verification even after a publish failure or a later freeze-variable change. A first-attempt 409 fails; only a same-invocation retry after an ambiguous prior attempt may treat an exact 409 as recovery.
- Given NuGet.org is queried before any authorized release, when the five Works IDs remain absent, then the result is reported as five 404s rather than as published.

## Implementation Notes

- Normalized `global.json` to the repository's CRLF convention with a final newline so the baseline-to-current diff passes the repository formatting gate.
- Serialized Release restore must disable MSBuild node reuse (`MSBUILDDISABLENODEREUSE=1` with `-nodeReuse:false`). A plain `dotnet restore` attaches to stale long-lived `/nodeReuse:true` worker nodes and hangs indefinitely on a futex after emitting only `Determining projects to restore...`; `dotnet build-server shutdown` hangs on the same nodes. This is a host-environment property, not a repository defect, and no build gate is weakened by the flag.

## Spec Change Log

- **Review loop 1 (2026-09-19):** Rows 46, 47, 49, 50, 52, 53, 58, 62, 66, 69, and 70 showed that the first plan could stamp only nuspecs, ignored dependency ranges, mixed the Operations source closure into Release, never compiled Debug locally, and treated the named mTLS restart race as port availability or a successful skip. The Code Map, tasks, acceptance criteria, and verification contract now require final-version compilation, assembly/range checks, a path-only Operations runtime resource, an explicit local Debug compile (CI remains Release-only per the Hexalith baseline), control-plane health, and no restart skips. Avoid the known-bad `pack --no-build` restamp, unconditional Operations `ProjectReference`, evaluation-only Debug proof, and port-only readiness model. **KEEP:** immutable Builds SHA `04d961759994396132bb2b113ee465b64740a543`; exact five-package manifest; manual non-cancelling exact-green-`main` release through protected `production`; explicit publication authority and secret checks; no `--skip-duplicate`; visible non-blocking NuGet audit; package-only consumer builds; all valid corrections from rows 2, 5, and 7-38 (immutable action pins, environment-scoped verification, repository-wide inventory governance, source-link symbols, fail-closed release checks, and bounded diagnostics); current root submodule pointers and the unrelated Aspire 13.5.4 update.
- **Review loop 2 (2026-09-20):** Rows 73-76, 79, 83-86, 89, 92-93, and 95, 97-99 showed that the second implementation could time out or pass with skipped facts, called ephemeral/local-ledger state byte-identical recovery, accepted first-attempt 409s without remote provenance, used raw DLL substring matching as assembly-version evidence, allowed teardown to mask or outlive a green fact, and kept critical post-publication branches inline and text-only tested. The Code Map, tasks, acceptance criteria, and verification contract now require a 60-minute CI budget, fail-on-skip runner configuration, bounded same-invocation retry provenance (first-attempt 409 remains a collision; cross-run recovery is not claimed), reflection/execution-based informational-version proof, primary-exception-preserving cleanup, and an executable post-publication script with adversarial tag/package tests. Avoid the known-bad 15-minute default, zero-exit skip acceptance, ephemeral cross-run recovery instructions, unconditional 409 acceptance, byte-string DLL inspection, mutable post-release freeze rechecks, and disposal exceptions that replace the body failure. **KEEP:** every Review-loop-1 KEEP item; both Debug/source and Release/package builds; exact five `.nupkg`/`.snupkg` pairs and checksum inventory; exact dependency-range and isolated-consumer validation; all three mTLS health waits; actionlint, Ruff, Node signature verification, CodeQL, and immutable workflow/action pins; the current root submodule pointers and unrelated Aspire update; no commit, push, release dispatch, publication, submodule update, or repository-setting side effect.

## Review Triage Log

Review iteration 1 (2026-09-19). Routing summary: 4 bad_spec (rows 1, 3, 4, 6), 33 patch, 2 defer, 1 rejected. Three layers ran: blind-hunter (16), edge-case-hunter (29), verification-gap (4 pre-verified + 6 other). Verdicts rendered after all layers reported. Severities assigned here; reviewer-assigned severities disregarded.

| # | Finding (location) | Verdict | Evidence | Route |
|---|---|---|---|---|
| 1 | Packaged assembly version != nuspec version (`scripts/pack-release-packages.py:96-115`) | high | Verified directly by opening `nupkgs/Hexalith.Works.Contracts.0.0.0-ci-test.nupkg`: nuspec `0.0.0-ci-test`, embedded `lib/net10.0/*.dll` carries `0.0.0-preview.0.221+61d292b2`. `--no-build` reuses compiled assemblies so `MinVerVersionOverride` restamps only the nuspec; `validate-nuget-packages.py` reads nuspec metadata only. | bad_spec |
| 2 | `HEXALITH_RELEASE_PUBLISH_ENABLED` scope mismatch (`.github/workflows/release.yml:107-134`) | high | Verified: `domain-release.yml@04d9617:262` sets `environment: ${{ inputs.environment-name }}`, so its `vars.` resolve in production scope; the caller's `verify-publication` declares no `environment`. An environment-scoped variable publishes while post-publication verification silently skips and reports success. Smallest fix is adding `environment: production` to the `verify-publication` job. | patch |
| 3 | Pinned package versions vs submodule source drift, ungated (`Directory.Build.props:17-24`) | high | Verified: `git describe` in `references/Hexalith.EventStore` = `v3.106.0-15-g9de3fc77`; catalog pins `HexalithEventStoreVersion` 3.106.0 (`references/Hexalith.Builds/Props/Directory.Packages.props:9`). Debug compiles 15 commits ahead of what Release packs. No test compares the two. | bad_spec |
| 4 | Debug source mode is never compiled in any verification path | high | Verified: `domain-ci.yml@04d9617:349` is the only build step and is `--configuration Release`; all `dotnet test` invocations are `--configuration Release --no-build`. The three Debug-aware fitness tests use `Microsoft.Build.Evaluation` item inspection only, never a compile. | bad_spec |
| 5 | P0 host-adapter assertion satisfied by fail-closed sentinel (`RuntimeAdapterGovernanceTests.cs:25-29`) | high | Verified: `KernelDependencyPolicy.cs:1169` returns `<conditional ProjectReference '...Hexalith.EventStore.DomainService.csproj'>` for a conditional declaration, and the weakened assertion is a `.Contains(...)` substring match, so the sentinel satisfies it. Would still pass if the condition were misspelled and resolved to nothing in both configurations. Smallest fix is per-configuration evaluation, which the sibling test in the same file already does. | patch |
| 6 | Release AppHost graph mixes packaged Aspire with source Operations chain (`Hexalith.Works.AppHost.csproj:18-23`) | medium | Confirmed from `deps.json`: `Hexalith.EventStore.Aspire/3.106.0 package` alongside `Hexalith.EventStore.Contracts/3.106.0 project` via the unconditional Operations reference. Operations is `IsPackable=false` so the split is forced, but Release is not purely package-based as claimed. | bad_spec |
| 7 | Repo-wide dependency sweep replaced by four hardcoded projects (`DependencyDirectionTests.cs:440-520`) | medium | Deleted `P0_HexalithDependenciesUseProjectReferencesNotPackageReferences` scanned every `Hexalith.Works*.csproj`; replacement enumerates four literal paths. A stray Hexalith `PackageReference` in Server/Projections/Reactor/Testing/ServiceDefaults/UnitTests/PropertyTests/ArchitectureTests is now ungated. | patch |
| 8 | AppHost opaque-declaration fail-closed guard deleted; topology asserted Release-only (`DependencyDirectionTests.cs:381-386`) | medium | The removed guard rejected declarations the exact-topology gate cannot evaluate; the remaining assertion runs one configuration. A Debug-only conditional ProjectReference on the AppHost passes every remaining gate. | patch |
| 9 | Teardown wait throws from `finally`, masking the real failure (`WorksAppHostSmokeHarness.cs:159-171`) | medium | `WaitForControlPlanePortsFreeAsync` is awaited in `finally` with the caller's token; a `TimeoutException`, or an `OperationCanceledException` now rethrown by the narrowed filter at `:456`, replaces the body's assertion failure. | patch |
| 10 | Startup-boundary wait converts a documented skip into a hard failure (`WorksAppHostSmokeHarness.cs:83`) | medium | The prerequisite path returned a skip reason naming the port; the new start-boundary call throws instead. The diff also deletes the doc comment recording why the gate skipped. | patch |
| 11 | `verify-publication` retry budget exceeds its own job timeout (`release.yml:140-197`) | medium | `timeout-minutes: 10` against 20 attempts x (5 curls at `--max-time 30 --retry 3` + 15s sleep). On genuinely missing packages the job dies on a generic timeout instead of emitting the constructed missing-package list. | patch |
| 12 | A no-op dispatch fails verification (`release.yml:150-176`) | medium | When semantic-release correctly publishes nothing, no tag resolves to the dispatched SHA, `match_count=0`, and the run fails though nothing went wrong. `governed-release: false` means no resolved-version output exists to gate on. | patch |
| 13 | Restore evidence is configuration-dependent and unguarded (`validate-nuget-packages.py:66`) | medium | Reproduced in-session: my Debug diagnostic build overwrote `src/Hexalith.Works.Contracts/obj/project.assets.json` with the source graph, after which the validator would compare Release nuspecs against Debug boundaries. It checks `projectPath`/`projectName` but never the restore configuration. | patch |
| 14 | Push glob wider than every validation gate; no partial-publish recovery path (`.releaserc.json`) | medium | `dotnet nuget push ./nupkgs/*.nupkg` would push a `*.symbols.nupkg` that all three validators explicitly exclude from counts. With `--skip-duplicate` deliberately absent, a mid-sequence push failure leaves a version permanently blocked by its own preflight, and no runbook is documented. | patch |
| 15 | NuGet audit cannot fail a build; README overstates it (`Directory.Build.props`, `README.md`) | low (corrected) | `NuGetAudit=true`/`NuGetAuditMode=all` are immediately paired with `NU1901;NU1902;NU1903;NU1904` in `WarningsNotAsErrors`, and no `NuGetAuditLevel` is set. **Triage correction:** the non-blocking behaviour is required, not defective -- `references/Hexalith.Builds/.github/workflows/ci-cd-standards.md:213-216` mandates NU1901-NU1904 in `WarningsNotAsErrors` so an unupgradable transitive advisory cannot block CI, and the frozen intent says keep the audit *visible*, not blocking. The patch dispatched for this row was wrong and was reverted; only the README overstatement was a real defect and it alone was fixed. | patch (README only) |
| 16 | Three shared workflows use mutable `@main` while CI/release pin the SHA | medium | `codeql.yml:21`, `commitlint.yml:20`, `dependency-review.yml:19` float on `@main`; `CiCdConfigurationTests` asserts the SHA pin only for `ci.yml`/`release.yml`. Upstream changes alter gate behavior with no change here. | patch |
| 17 | README CI Release block omits the submodule prerequisite (`README.md`) | medium | Release still hard-requires `references/Hexalith.EventStore` for the unpackageable Operations project, but the CI block opens with `dotnet restore` and no `git submodule update --init`. Run verbatim on a fresh clone it fails. | patch |
| 18 | A fitness test pins the README to a self-invalidating dated claim (`CiCdConfigurationTests.cs:130-141`) | medium | `Occurrences(readme, "(HTTP 404)").ShouldBe(5)` hardcodes the unpublished state. After the first real release the README must keep a false claim to keep the gate green. | patch |
| 19 | `ci.yml` cancels in-progress runs on `main` pushes | medium | `cancel-in-progress: true` on `ci-${{ github.ref }}` can cancel exactly the push-CI run that release gating later requires to exist for that SHA. | patch |
| 20 | No gate asserts the packable-project set equals the manifest five | medium | The manifest->project direction is checked; project->manifest is not. A new `IsPackable=true` library is publishable without appearing in `tools/release-packages.json`. | patch |
| 21 | Published packages ship no symbols and no SourceLink | medium | No `IncludeSymbols`/`SymbolPackageFormat`/`PublishRepositoryUrl`/`EmbedUntrackedSources` despite declaring `RepositoryUrl`. | patch |
| 22 | Bare build with no Configuration silently selects package mode (`Directory.Build.props:18`) | medium | The final fallback sets `UseHexalithProjectReferences=false` whenever `Configuration` is unset, so a bare `dotnet build` resolves package references while producing Debug output. | patch |
| 23 | Unresolvable tag aborts verification under `set -e` (`release.yml:163-169`) | low | `gh api repos/.../commits/<tag>` on a deleted tag exits non-zero and kills the step after packages are live. | patch |
| 24 | Prerelease versions accepted by pack/preflight are rejected by verification (`release.yml:165`) | low | `^v[0-9]+\.[0-9]+\.[0-9]+$` excludes the prerelease suffix that `VERSION_PATTERN` and the preflight both allow. | patch |
| 25 | `printf` argument/placeholder mismatch mislabels success output (`release.yml:198`) | low | `printf 'Verified %s %s\n' "${package_ids[@]}" "$VERSION"` passes 6 arguments to 2 placeholders, so the format recycles and pairs package IDs with each other. | patch |
| 26 | Tautological guard in consumer validation (`validate-consumer-package-references.py:118-119`) | low | `write_consumer` checks `"ProjectReference" in project.read_text()` against the literal template it just wrote, so it can never fire. The real proof is `assert_package_only`. | patch |
| 27 | Consumer script forks the manifest (`validate-consumer-package-references.py:22-28`) | low | `PACKAGE_PROBES` hardcodes the five IDs instead of reading `tools/release-packages.json`, unlike the other two scripts, so it alone will not notice a manifest change. | patch |
| 28 | Approved SHA and package count duplicated across six files with no agreement check | low | `04d9617...` appears in 4 files (5 sites) and the literal `5` in 5 files; nothing asserts the copies agree or derives them from the manifest. | patch |
| 29 | `commitlint.yml` passes an empty title on the push trigger (`commitlint.yml:8,21`) | low | `github.event.pull_request.title` is null on `push`, so the title lint silently passes. | patch |
| 30 | Analyzer metadata on a `PackageReference` is inert (`Hexalith.Works.Contracts.csproj:16`) | low | `OutputItemType`/`ReferenceOutputAssembly` are `ProjectReference` metadata and ignored by NuGet. Harmless today (the 1.19.3 package ships only `analyzers/dotnet/cs`), but expresses a control that is not in effect. | patch |
| 31 | Contradictory explicit dependency-mode properties are unreconciled (`Directory.Build.props:15-22`) | low | Setting both `UseHexalithProjectReferences=true` and `UseNuGetDeps=true` is accepted; sequential evaluation silently picks one. | patch |
| 32 | `ExternalProjectReferences` filter hardcodes the `references/Hexalith.` path fragment (`DependencyDirectionTests.cs:665-670`) | low | `Directory.Build.props` still supports a sibling `..\Hexalith.EventStore` checkout, where the filter matches nothing and both mode assertions become vacuous. | patch |
| 33 | `Occurrences(...) == 1` cannot distinguish a blocking tier from a disabled one (`CiCdConfigurationTests.cs`) | low | A commented-out path, or one moved to a non-blocking input key, still counts as exactly one occurrence. | patch |
| 34 | Preflight hardcodes the repository slug (`validate-publication-preflight.sh:31`) | low | `[[ "$repository" = "Hexalith/Hexalith.Works" ]]` fails on any fork. Deliberate fail-closed choice, but undocumented. | patch |
| 35 | Release job grants `issues: write` / `pull-requests: write` unnecessarily | low | `.releaserc.json` sets `successCommentCondition: false`, `labels: []`, `releasedLabels: false`, so the grants exceed least privilege. | patch |
| 36 | Node toolchain is not locally runnable (`package.json`) | low | No `scripts` block, `.nvmrc`, or `packageManager`; no workflow in this repo runs `npm ci`, so commitlint/semantic-release cannot be exercised locally. | patch |
| 37 | `dependency-review.yml` passes no configuration | low | No `fail-on-severity`, no `allow-licenses`/`deny-licenses` despite the repo declaring `PackageLicenseExpression=MIT`. | patch |
| 38 | New scripts have no linting or tests | low | The five `scripts/*.py|sh` have no ruff/shellcheck step and no unit coverage of their own. | patch |
| 39 | `suppressParent: all` not filtered from central transitive groups (`validate-nuget-packages.py:107`) | maybe-false | `dependencies.update(central)` adds every central transitive id unconditionally while direct dependencies filter `suppressParent`. Would need a project that actually carries a suppressed central transitive entry to confirm a false mismatch; none currently does. | defer |
| 40 | Cascade lane does not adopt the new control-plane waits (`WorksCascadeRecoveryPipelineSmokeTests.cs:543-580`) | medium | Independently confirmed earlier this session; only 2 of 5 AppHost-starting files use the harness. Already logged to `deferred-work.md` under the human's 2026-09-19 scope decision. | defer (logged) |
| 41 | Publication is ungated by `HEXALITH_RELEASE_PUBLISH_ENABLED` (blind-hunter) | false | Refuted: `domain-release.yml@04d9617:401-416` reads `vars.HEXALITH_RELEASE_PUBLISH_ENABLED` in its own `publish-gate` step and skips semantic-release unless it is exactly `true`. Publication is gated; the real defect is the scope mismatch recorded at row 2. | rejected |

Review iteration 2 (2026-09-19). Routing summary: 11 bad_spec, 15 patch, 3 defer, 1 rejected. Nine exact repeats were carried from the first review. Three layers ran in parallel: blind-hunter (16), edge-case-hunter (8), and verification-gap (5 pre-verified + 1 other). Lower patch/defer entries are preserved for re-derivation but are moot until the bad-spec loop completes.

| # | Finding (location) | Verdict | Evidence | Route |
|---|---|---|---|---|
| 42 | Primary package pushes implicitly push matching symbols, then the script pushes each `.snupkg` again (`scripts/push-release-packages.sh:41-58`) | high | Verified with the installed `dotnet nuget push --help`: a matching symbol package is pushed unless `--no-symbols` is supplied. The later explicit `.snupkg` push can collide and fail an otherwise successful release. | patch |
| 43 | Documented partial-publication recovery aborts on the first already-published artifact (`scripts/push-release-packages.sh:10-15,55-58`; `README.md:77-80`) | high | Verified: `set -e` and the unguarded push exit on a 409, so an unchanged retry never reaches the remaining artifacts. The recovery text promises behavior the script does not implement. | patch |
| 44 | Post-publication inventory verification is skipped after a partially successful publish fails (`.github/workflows/release.yml:93-101`) | high | Verified: `verify-publication` has a default-success `needs: release` dependency. A mid-sequence NuGet failure makes the workflow red but never runs the required missing-ID report. | patch |
| 45 | Post-publication verification does not independently observe symbol-service availability (`.github/workflows/release.yml:173-225`) | maybe-false | Package pushes wait for upload acceptance, but NuGet symbol validation can be asynchronous. Settling this requires an authoritative symbol-validation status endpoint or evidence that successful CLI return guarantees indexed symbols; defer rather than inventing a probe. | defer |
| 46 | Package metadata can be restamped after the DLL was compiled (`scripts/pack-release-packages.py:91-110`) | high | Carried from row 1: the script still uses `dotnet pack --no-build` while supplying the final version, so the already-built assembly can retain a different MinVer version. | bad_spec |
| 47 | Package validation discards dependency version ranges (`scripts/validate-nuget-packages.py:152-225`) | high | Verified: `PackageMetadata.dependencies` retains only IDs and compares only those IDs with the restore-derived boundary. A stale or widened nuspec range can pass despite the acceptance requirement for correct NuGet dependencies. | bad_spec |
| 48 | Symbol packages are checked only by filename (`scripts/pack-release-packages.py:118-127`) | low | The gate would accept a malformed matching archive, but the normal producer is the immediately preceding successful `dotnet pack`. Deep PDB/SourceLink inspection adds disproportionate machinery for an undemonstrated everyday failure. | rejected |
| 49 | Release AppHost mixes packaged Aspire with the source Operations closure (`src/Hexalith.Works.AppHost/Hexalith.Works.AppHost.csproj:18-22`) | medium | Carried from row 6: Operations remains an unconditional source project and brings source assemblies into the Release topology beside the pinned packages. | bad_spec |
| 50 | CI never compiles the Debug sibling-source graph (`.github/workflows/ci.yml:18-30`) | high | Carried from row 4: the shared lane builds Release only, and architecture tests evaluate Debug items without compiling them. | bad_spec |
| 51 | Other live AppHost suites do not use the repaired harness (`WorksCascadeRecoveryPipelineSmokeTests.cs:543-580`) | medium | Carried from row 40: the cascade suite still owns a separate start/dispose helper with no port-settling behavior. | defer |
| 52 | The harness waits for ports but not mTLS control-plane health (`WorksAppHostSmokeHarness.cs:83-155`) | high | Verified against the frozen intent and observed failure: readiness waits only for `eventstore` and `works`; a bound-but-unhealthy placement or scheduler remains unresolved until the five-minute timeout. | bad_spec |
| 53 | Restart-boundary port contention becomes a successful test skip (`WorksAppHostSmokeHarness.cs:90-97`) | high | Verified: after the one prerequisite probe, every later timeout calls `Assert.Skip`; xUnit can return success without executing the restart assertions. Distinguishing initial foreign contention from teardown leakage needs a specified lifecycle contract. | bad_spec |
| 54 | Dependency-mode validation permits false/false, non-boolean, and disagreeing source flags (`Directory.Build.props:17-28,74-82`) | medium | Verified: only true/true is rejected. False/false silently resolves to packages, and pre-set `Hexalith*FromSource` properties can contradict both switches. | patch |
| 55 | CI installs an unpinned Ruff version (`.github/workflows/ci.yml:43-44`) | low | Verified: `pipx install ruff` resolves the latest release, so an unchanged commit can fail after a Ruff release. Pinning is a direct correction. | patch |
| 56 | Actionlint is a manual acceptance check but not a CI gate (`.github/workflows/ci.yml:32-46`) | medium | Verified: neither the caller nor pinned `domain-ci.yml` runs actionlint. A workflow syntax or expression regression can merge without the stated workflow validation. | patch |
| 57 | CodeQL omits the new Python release tooling (`.github/workflows/codeql.yml:25`) | low | Verified: the reusable scan receives only `csharp`, leaving the publication-control Python scripts outside SAST. Adding the supported Python language is a direct correction. | patch |
| 58 | Restart-boundary port contention is converted into an accepted skip (verification-gap) | high | Pre-verified: the shared CI treats an exit-zero project as passing and does not reject skipped facts; no focused test covers the timeout classification. This is the same lifecycle-contract defect as row 53. | bad_spec |
| 59 | Publication collision handling has no executable regression test (verification-gap) | low | Carried from row 38: the release scripts still have lint/static-string checks but no subprocess test that proves mixed 404/200 responses fail before publication. | patch |
| 60 | Package rejection branches run only against valid generated packages (verification-gap) | low | Carried from row 38: no malformed fixture tests exercise missing metadata, dependency drift, or forbidden dependency failures. | patch |
| 61 | Cascade restart does not adopt the shared port-settling harness (verification-gap) | medium | Carried from row 40: the same local helper and missing adoption remain, and the work is already present in the deferred ledger. | defer |
| 62 | The Debug source dependency graph is never compiled in CI (verification-gap) | high | Carried from row 4: item evaluation does not prove the graph compiles, and the pinned CI lane builds Release only. | bad_spec |
| 63 | Partial-publication recovery exits on the first 409 (verification-gap other finding) | high | Verified: the unguarded push loop contradicts the script and README recovery instructions and leaves later unpublished artifacts unreachable. | patch |
| 64 | Invalid or empty manifests can produce a zero-artifact successful publish (`scripts/push-release-packages.sh:34-39`) | high | Verified: the `jq` command runs in process substitution, whose failure is not the `mapfile` command's status. `mapfile` can succeed with an empty array and the script then reports zero artifacts published. | patch |
| 65 | A recovery retry cannot move past a previously published artifact (`scripts/push-release-packages.sh:55-58`) | high | Verified: no exact-409 branch exists, so `set -e` stops the loop before remaining artifacts. Same root cause as rows 43 and 63. | patch |
| 66 | Semantic version is supplied only to a no-build pack (`scripts/pack-release-packages.py:91-110`) | high | Carried from row 1: the current script can still produce a nuspec version that disagrees with the embedded assembly metadata. | bad_spec |
| 67 | False/false or non-boolean dependency switches are accepted (`Directory.Build.props:17-82`) | medium | Verified: the only error condition is true/true, and the fallback silently selects package mode for all other contradictory values. | patch |
| 68 | A NuGet probe transport error aborts the outer publication poll (`.github/workflows/release.yml:198-215`) | medium | Verified: under `set -e`, the unguarded `status="$(curl ...)"` exits the step on curl failure, bypassing the constructed per-package missing report. | patch |
| 69 | Nuspec dependency versions are not validated (`scripts/validate-nuget-packages.py:129-225`) | high | Verified: dependency elements are reduced to IDs before comparison, so ranges and exact versions never participate in the release gate. Same root cause as row 47. | bad_spec |
| 70 | Scheduler/placement health is absent from readiness (`WorksAppHostSmokeHarness.cs:141-155`) | high | Verified: only `eventstore` and `works` are awaited, so the exact bound-but-unhealthy control-plane race named by the intent is not repaired. Same root cause as row 52. | bad_spec |
| 71 | Package-version origin policy is tested only with fixtures, not applied to repository projects (`DependencyDirectionTests.cs:581-720`) | medium | Verified: `EvaluateHexalithSourceConsumption` detects out-of-catalog `PackageVersion` items in temporary projects, but no current test invokes it over the actual Works project set. | patch |
| 72 | CI leaves the combined job at the shared 15-minute default although the live suite took 25m34s (`ci.yml:20-32`) | high | Verified at pinned Builds SHA: `build-timeout-minutes` defaults to 15 and governs the single build-and-test job; the accepted local Integration run alone took 25m34.651s. | patch |
| 73 | Blocking CI accepts skipped xUnit facts (`ci.yml`; pinned `domain-ci.yml:403-558`) | high | Verified: the MTP lanes use only `dotnet test` exit status and never pass `--fail-skips on` or inspect TRX skip counts, while several live Works facts call `Assert.Skip`; exact-source CI can therefore be green without exercising required topology facts. | bad_spec |
| 74 | The verify preflight makes a cross-run partial-publication retry collide before the publisher recovery branch (`.releaserc.json:12-14`) | high | Verified: `verifyReleaseCmd` rejects every existing ID/version before `publishCmd`; the README's rerun path can never reach the script's 409 branch after any package became live. | bad_spec |
| 75 | Recovery candidates and their checksum ledger disappear with the failed hosted runner (`pack-release-packages.py:123-139`) | high | Verified: non-governed `domain-release.yml` uploads neither `nupkgs` nor the ledger after semantic-release failure, so a new run cannot prove it is retrying the frozen candidate bytes. | bad_spec |
| 76 | Any textual 409/Conflict is called byte-identical recovery without proving the remote artifact (`push-release-packages.sh:70-88`) | high | Verified: the ledger authenticates only local files and the first push attempt can return 409 for content published by another principal. No remote digest or same-invocation prior-attempt condition binds the collision to this candidate. | bad_spec |
| 77 | The publisher can target a registry different from both collision and post-publication probes (`push-release-packages.sh:15`) | medium | Verified: `HEXALITH_RELEASE_NUGET_SOURCE` redirects writes while both probes are hard-wired to NuGet.org, allowing an explicit invocation to mutate another registry and then report against NuGet.org. | patch |
| 78 | Symbol archives are accepted by filename without inspecting their debugging contents (`pack-release-packages.py:119-128`) | low | carried: row 48 already verified and rejected the same claim; the producer is the immediately preceding successful SDK pack and deep PDB/SourceLink inspection remains disproportionate for this story. | rejected (carried row 48) |
| 79 | Assembly-version validation searches arbitrary DLL bytes (`validate-nuget-packages.py:210-226`) | medium | Verified: any unrelated occurrence of the requested version satisfies the check; the fixture is deliberately not a managed PE, so tests cannot prove `AssemblyInformationalVersion`. | bad_spec |
| 80 | A nuspec dependency missing ID/version is silently dropped (`validate-nuget-packages.py:183-193`) | low | Verified: the comprehension filters malformed elements out, so an unexpected versionless dependency can disappear before exact-set comparison. Rejecting malformed dependency elements is a direct correction. | patch |
| 81 | Dependency comparison unions framework groups (`validate-nuget-packages.py:129-193`) | low | The concern is valid only after a release project multi-targets; every current manifest project has one inherited `net10.0` target, so no current consumer can receive a wrong per-TFM group. Per-framework machinery is not justified here. | rejected |
| 82 | Package validation accepts any license metadata rather than exact MIT (`validate-nuget-packages.py:195-265`) | low | Verified as a future governance hole: a project override can replace the root MIT expression and still pass. Exact expression comparison is a direct correction. | patch |
| 83 | A successful live-test body stays green when teardown leaves fixed resources occupied (`WorksAppHostSmokeHarness.cs:180-208`) | medium | Verified: every settling exception becomes only a diagnostic, including on the last fact where no later prerequisite probe exposes the leak. | bad_spec |
| 84 | App/builder disposal can replace the original body exception (`WorksAppHostSmokeHarness.cs:180-188`) | medium | Verified: both disposal awaits run in `finally` without capturing the active body exception; either can become the exception observed by xUnit. | bad_spec |
| 85 | Root submodule pointer changes violate this implementation's frozen boundary (`references/Hexalith.*`) | false | Refuted for this resumed implementation: those pointers are committed pre-existing branch state, the current run performed no submodule update, and Review-loop-1 explicitly requires preserving the current pointers. Reversing them would overwrite unrelated human work. | rejected |
| 86 | Post-publication tag/package behavior is only source-text checked (`release.yml:131-235`) | high | Pre-verified by the verification-gap layer: no executable test drives matching, unrelated, unresolvable, zero, or multiple tag branches, and changing the SHA match can leave actionlint/Architecture green while package verification is skipped. | bad_spec |
| 87 | Non-boolean `UseNuGetDeps` has no symmetric executable test (`BuildConfigurationTests.cs:91`) | low | Pre-verified: the guard exists, but the theory covers only non-boolean `UseHexalithProjectReferences`. One additional row directly closes the regression gap. | patch |
| 88 | Configuration-free package-mode fallback and `HXW0002` are untested (`Directory.Build.props:27-33`) | low | Pre-verified: every evaluator/build supplies Debug or Release and searches find no assertion for `HXW0002`. An executable no-configuration MSBuild fact is a direct correction. | patch |
| 89 | No test reads real managed assembly informational metadata (`validate-nuget-packages.py:210-226`) | medium | Pre-verified: the negative fixture is arbitrary bytes and isolated consumers only compile, so a stale informational version plus an unrelated matching string passes all current checks. | bad_spec |
| 90 | Symbol packages lack PDB/SourceLink content verification (`pack-release-packages.py:118`) | low | carried: row 48 already verified and rejected the same filename-only symbol claim; no new bad outcome or changed producer was shown. | rejected (carried row 48) |
| 91 | A project can assign non-boolean final dependency modes after `Directory.Build.props` captured caller inputs (`Directory.Build.props:81-111`) | medium | Verified: for example, later values `UseHexalithProjectReferences=maybe`, `UseNuGetDeps=true`, and both source flags `maybe` evade requested-value checks, unequal-mode check, and source agreement checks. | patch |
| 92 | A true-to-false freeze-variable edit between protected jobs suppresses post-publication verification (`release.yml:93-120`) | high | Verified: the release job can publish after reading `true`, then the second job rereads `false` and skips checkout, tag resolution, and every package probe. | bad_spec |
| 93 | An unresolvable dispatch tag can be misclassified as no release warranted (`release.yml:145-171`) | high | Verified: lookup failure is skipped; if it is the dispatch tag, `match_count=0` returns success and bypasses all package checks. | bad_spec |
| 94 | A final five-package poll can overrun the nominal 420-second deadline (`release.yml:203-227`) | medium | Verified: the deadline is checked only after five sequential requests, so the last iteration can add up to 75 seconds plus prior tag/setup time inside a ten-minute job. | patch |
| 95 | A concurrent different publication can turn a retry into a falsely accepted exact 409 (`push-release-packages.sh:63-89`) | high | Verified: there is no requirement that this invocation made an earlier ambiguous attempt for that artifact, and no remote identity evidence; local hashes alone cannot attribute the 409. | bad_spec |
| 96 | Missing nuspec dependency versions are not rejected (`validate-nuget-packages.py:183-193`) | low | Verified; same root cause as row 80. The element is filtered rather than diagnosed, allowing malformed unexpected metadata to vanish from comparison. | patch |
| 97 | Assembly metadata can be stale while an unrelated byte occurrence satisfies validation (`validate-nuget-packages.py:210-226`) | medium | Verified; same root cause as rows 79 and 89. Exact reflection/metadata inspection is required by the acceptance claim. | bad_spec |
| 98 | Executable release tests do not exercise transient retry then success or exhaustion (`test_release_tooling.py:168-265`) | medium | Verified: the transport stub returns one failure and the publisher suite covers 409 continuation, changed bytes, and empty manifests, but no retry state transition or attempt budget. | bad_spec |
| 99 | Disposal/probe failures can replace the live body failure (`WorksAppHostSmokeHarness.cs:180-208`) | medium | Verified; same root cause as row 84. The body exception is not retained outside the inner catch and cleanup exceptions are not aggregated conditionally. | bad_spec |
| 100 | Port availability uses connect rather than exclusive bind (`WorksAppHostSmokeHarness.cs:522-539`) | low | Verified: a bound non-listening TCP socket rejects connect and is reported free, after which AppHost cannot bind it. Replacing the probe with an exclusive bind is a direct correction. | patch |
| 101 | Stopped Scheduler containers must count as volume owners (`WorksAppHostSmokeHarness.cs:489-519`) | false | Refuted: `docker ps` intentionally reports active mounts; a stopped container does not own the volume mount or block a new scheduler container, while `--all` would make harmless retained containers block forever. | rejected |


## Design Notes

Works has no container package contract, so release uses the FrontComposer-style NuGet-only path while retaining the simpler Tenants/EventStore manifest and isolated-consumer checks. The current Builds publication freeze remains fail-closed; repository owners must separately configure `production`, `NUGET_API_KEY`, and `HEXALITH_RELEASE_PUBLISH_ENABLED=true` before a later authorized dispatch.

## Verification

**Commands:**
- `actionlint .github/workflows/*.yml && npm ci --ignore-scripts && npm audit signatures && bash scripts/lint-scripts.sh` -- workflow, Node supply-chain, and pinned shell/Python tooling validation passes.
- `python3 -m unittest discover -s scripts/tests` (or the selected repository-consistent runner) -- release-tooling negative paths execute, including malformed manifests/packages, collisions, transport failures, and partial-publication retries.
- `dotnet restore Hexalith.Works.slnx -p:Configuration=Debug && dotnet build Hexalith.Works.slnx --configuration Debug --no-restore -warnaserror -m:1` -- mandatory local-only sibling-source compile passes; this is not added to CI, which remains Release/package-only under the Hexalith baseline.
- `dotnet restore Hexalith.Works.slnx -p:Configuration=Release && dotnet build Hexalith.Works.slnx --configuration Release --no-restore -warnaserror -m:1` -- package-mode Release gate passes with zero warnings/errors and no compile-time EventStore Operations source closure.
- `python3 scripts/pack-release-packages.py ./nupkgs 0.0.0-ci-test && python3 scripts/validate-nuget-packages.py ./nupkgs && python3 scripts/validate-consumer-package-references.py ./nupkgs` -- the final-version build produces exact five package/symbol pairs whose assembly versions and dependency ranges match Release evidence, and all five isolated consumers compile.
- `for project in tests/Hexalith.Works.UnitTests tests/Hexalith.Works.ArchitectureTests tests/Hexalith.Works.PropertyTests tests/Hexalith.Works.IntegrationTests; do dotnet test "$project" --configuration Release --no-build; done` -- every project passes separately with zero skips; use direct xUnit `-method` for restart diagnosis without substituting it for the broad gate.
- `curl` NuGet flat-container indexes for every manifest ID -- current status is explicitly captured; after a future authorized release, every exact version must return HTTP 200.

**Observed results (2026-09-18):**
- `actionlint`, shell/Python/JSON validation, `npm ci --ignore-scripts`, `npm audit signatures`, and the CRLF-aware `git -c core.whitespace=cr-at-eol diff --check` gate passed. NPM installed 505 packages with zero vulnerabilities and verified 504 registry signatures plus 127 attestations.
- Release restore/build passed with zero warnings and errors. Exactly five `0.0.0-ci-test` packages passed metadata/dependency validation, and all five isolated PackageReference-only consumers built successfully.
- Unit passed 568/568, Architecture passed 246/246, and Property passed 3/3. NuGet.org returned HTTP 404 for each of the five manifest package IDs.
- Integration completed 510/516. All six live AppHost failures occurred after Dapr Sentry exited with `failed to add target /var/run/dapr/credentials: no space left on device`; the focused `WorksReminderRecoveryPipelineSmokeTests.Recovery_re_registers_a_still_future_await_that_later_fires` run failed for the same reason before the application became healthy.
- At the failure boundary, `/tmp` had zero free inodes and host inotify use was 1,048,538 of 1,048,576 watches. No unrelated processes or temporary data were modified. The full integration and focused restart acceptance gates remain environment-blocked and must pass before this spec can move to review.
- Reverification after capacity partially recovered passed the focused restart regression 1/1 with no skips in 355.648 seconds. The broad Integration project then completed 511/516 with no skips; all five live AppHost failures reported `eventstore` as `FailedToStart`, while the other 511 tests passed.
- The second broad-run failure boundary had 1,025,414 of 1,048,576 inotify watches in use (97.8%), principally held by unrelated VS Code file watchers and language/MCP services. Fixed AppHost control-plane ports were released and no test-owned DCP processes or containers remained. The broad Integration acceptance gate remains environment-blocked; no unrelated process was terminated.

**Reverification (2026-09-19):**
- Static checks, Release restore/build, the exact five-package validation, and five isolated package-only consumer builds passed. Unit passed 568/568, Architecture passed 246/246, Property passed 3/3, and all five manifest package IDs still returned HTTP 404 from NuGet.org.
- Integration completed 514/516 with no skips. The two live-topology failures were a Works Dapr actor-host metadata timeout while waiting for scheduler/placement connectivity and EventStore reporting `FailedToStart`.
- Host inotify allocation was 1,048,164 of 1,048,576 watches (99.96%), predominantly held by unrelated VS Code and MCP processes. Test-owned DCP resources were cleaned up and the fixed control-plane ports were released; no unrelated process was terminated. The broad Integration acceptance gate remains environment-blocked, so status remains `in-progress`.

**Latest reverification (2026-09-19):**
- `actionlint`, Python/shell/JSON validation, `npm ci --ignore-scripts`, `npm audit signatures`, Release restore/build, exact five-package validation, and all five isolated package-only consumer builds passed. Unit passed 568/568, Architecture passed 246/246, and Property passed 3/3, all without skips. The five manifest package indexes still returned HTTP 404 from NuGet.org.
- After the CRLF-preserving `global.json` final-newline normalization, serialized Release restore with build-server reuse disabled passed and the Architecture assembly passed again at 246/246 with no skips.
- Integration completed 510/516 with no skips. All six live-topology facts reached the same pre-application boundary: the AppHost `eventstore` resource reported `FailedToStart`. The focused restart regression ran directly as 1/1 and failed at that same boundary.
- Aspire resource/log inspection identified the upstream failure: `dapr-sentry` exited with `failed to add target /var/run/dapr/credentials: no space left on device`. The host had 1,048,534 of 1,048,576 inotify watches allocated (42 free), while `/tmp` still had 77,817 filesystem inodes free. No unrelated watcher-owning process was terminated; the diagnostic AppHost was stopped cleanly, fixed control-plane ports were released, and the pre-existing Dapr placement/scheduler containers were restored to their original running state. The broad Integration acceptance gate remains environment-blocked, so status remains `in-progress`.

**Acceptance reverification (2026-09-19, all gates green):**
- `actionlint .github/workflows/*.yml`, Python/shell/JSON/YAML syntax validation, `npm ci --ignore-scripts` (505 packages audited, 0 vulnerabilities), and `npm audit signatures` (504 verified registry signatures, 127 attestations) passed. The CRLF-aware `git -c core.whitespace=cr-at-eol diff --check` baseline-to-HEAD gate stayed clean.
- Release restore and `dotnet build Hexalith.Works.slnx --configuration Release --no-restore -warnaserror -m:1` passed with **0 warnings and 0 errors**, using package-mode external references.
- `pack-release-packages.py` produced exactly the five manifest packages at `0.0.0-ci-test`; `validate-nuget-packages.py` confirmed one shared version, license/README metadata, and no host/sample/test dependency leakage; `validate-consumer-package-references.py` built all five isolated PackageReference-only consumers with 0 warnings and 0 errors.
- Every test project ran separately from its built xUnit v3 Release binary: **Unit 568/568, Architecture 246/246, Property 3/3, Integration 516/516 [the Integration figure did not reproduce — see the correction below]** — 0 errors, 0 failures and **0 skips** in all four. Integration took 1371.706s and exercised the live AppHost topology, so `WorksReminderRecoveryPipelineSmokeTests.Recovery_re_registers_a_still_future_await_that_later_fires` executed and passed inside the broad run rather than being skipped.
- [SUPERSEDED — see the correction below] The broad Integration acceptance gate that was previously environment-blocked now passes. The host `fs.inotify.max_user_watches` limit is 4,194,304 with roughly 1.05M allocated (about 3.1M free), so `dapr-sentry` started normally instead of exiting with `failed to add target /var/run/dapr/credentials: no space left on device`. Test-owned `dapr-sentry`/`dapr-placement-mtls`/`dapr-scheduler-mtls` DCP containers were torn down, ports 50001/51005/51006 were released, and the pre-existing `dapr init` containers (`dapr_placement`, `dapr_scheduler`, `dapr_redis`, `dapr_zipkin`) were left running and untouched. No unrelated process was terminated.
- NuGet.org flat-container indexes for `Hexalith.Works.Contracts`, `Hexalith.Works.Server`, `Hexalith.Works.Projections`, `Hexalith.Works.Reactor`, and `Hexalith.Works.Testing` each returned **HTTP 404**. The five IDs remain unpublished; this is recorded as status only and is not publication authority. No commit, push, repository-configuration change, or release dispatch was performed.
- Contract cross-check against the pinned Builds commit `04d961759994396132bb2b113ee465b64740a543`: `domain-ci.yml` and `domain-release.yml` at that exact SHA declare every input the callers pass, and the release workflow exports `HEXALITH_BUILDS_EXECUTION_SHA`, `HEXALITH_RELEASE_ENVIRONMENT`, `HEXALITH_RELEASE_SOURCE_BRANCH`, `HEXALITH_RELEASE_SOURCE_CI_WORKFLOW`, `HEXALITH_RELEASE_PACKAGE_MANIFEST`, and `HEXALITH_RELEASE_EXPECTED_PACKAGE_COUNT`, which are exactly the variables `scripts/validate-publication-preflight.sh` fails closed on.

**Verification correction (2026-09-19, independent re-run):**
- The host `fs.inotify.max_user_watches` limit was raised from 1,048,576 to 4,194,304 (runtime-only `sysctl -w`, not persisted) with the human's explicit approval, after confirming all 1,048,536 allocated watches were held by unrelated VS Code, C# Dev Kit, and `fluentui-mcp` processes. No process was terminated. This removed the `dapr-sentry` `no space left on device` failure that had blocked the gate on four prior attempts.
- Architecture was independently re-run and confirmed at 246/246 with 0 skips. The five manifest package IDs were independently re-queried and each returned HTTP 404.
- The `Integration 516/516` result recorded above **did not reproduce**. An independent broad run completed **515/516, 0 skipped**, with one failure: `WorksCascadeRecoveryPipelineSmokeTests.Reactor_translators_run_live_and_interrupted_cascade_converges_after_restart` — `WaitForEventCountAsync(s_secondChild, WorkItemCancelled, 1)` returned 0 against a 60-second budget, at `WorksCascadeRecoveryPipelineSmokeTests.cs:89` ("startup checkpoint replay must terminate the outstanding child"). Phase 1 passed; phase 2 (post-restart replay) failed.
- The failure is **reproducible and not caused by this spec**. It failed identically in the broad Release run, in an isolated Release run, and in an isolated **Debug source-mode** run (3/3). Because Debug consumes EventStore and PolymorphicSerializations from sibling source, the Release package-mode switch introduced by this spec is excluded as the cause.
- Root cause of its newly-visible status: the inotify ceiling previously killed the AppHost at `eventstore: FailedToStart` before this fact's logic executed, so the defect was masked rather than absent. `CascadeRecoveryService.ExecuteAsync` swallows any reconciler exception into a `RecoveryStepFailed` log line, so a failed replay pass surfaces only as a zero event count.
- Related gap found while diagnosing: `WorksCascadeRecoveryPipelineSmokeTests` declares its own `WithAppHostAsync` (`WorksCascadeRecoveryPipelineSmokeTests.cs:576-580`) whose `finally` disposes and returns without settling the fixed control-plane ports. The harness repair delivered by this spec therefore does not reach this restart fact. Of the five Integration files that start an AppHost, only `WorksMtlsAuthorizationSmokeTests` and `WorksReminderRecoveryPipelineSmokeTests` route through `WorksAppHostSmokeHarness`.
- Acceptance criterion 2 ("Unit, Architecture, Property, and Integration suites pass") is therefore **not satisfied**, and the status remains `in-progress`. Repairing the Story 4.7 cascade startup-replay path is outside this spec's frozen intent, which scopes test repair to the SDK assertion drift and the AppHost restart race; it needs a human scope decision.
- Human scope decision (2026-09-19): the cascade startup-replay defect and the harness-coverage gap are deferred to `deferred-work.md` as separate items rather than renegotiating this spec's frozen intent. The inotify limit stays runtime-only and was not persisted.
- Subsequent host remediation, explicitly requested by the human, persisted `/etc/sysctl.d/99-hexalith-inotify.conf` with `fs.inotify.max_user_watches=8388608`, `fs.inotify.max_user_instances=16384`, and `fs.inotify.max_queued_events=65536`. Approximately 1.08M watches remained allocated to unrelated active IDE/MCP services, leaving about 7.31M free; those processes were deliberately left untouched.
- After that capacity increase, `dotnet test tests/Hexalith.Works.IntegrationTests/Hexalith.Works.IntegrationTests.csproj --configuration Release --no-build` passed **516/516 with 0 failures and 0 skips** in 23m 48.706s. The test-owned Aspire/Dapr topology exited cleanly and fixed control-plane ports 50001/51005/51006 were released. The separately deferred cascade replay and harness-coverage work remains recorded in `deferred-work.md` despite this green rerun.

**Post-review verification (2026-09-19, after 34 patches):**
- Release restore and build passed with 0 warnings and 0 errors. Architecture **248/248** (+2 new gates), Unit **568/568**, Property **3/3**, all with 0 skips. `actionlint`, `npm ci --ignore-scripts` (0 vulnerabilities) and `npm audit signatures` (127 attestations) passed. Pack produced exactly five `.nupkg` plus five matching `.snupkg`; metadata/dependency validation and all five isolated package-only consumers passed. The five manifest IDs still return HTTP 404.
- Triage correction on the NuGet audit row: the dispatched patch made NU1903/NU1904 blocking, which contradicts `ci-cd-standards.md:213-216` (an authoritative `context:` file) and the frozen "keep NuGet audit visible" constraint. The patch was reverted, all four advisory codes were restored to `WarningsNotAsErrors`, and only the README overstatement -- the genuine defect -- was fixed. `P0_NuGetAuditRemainsVisibleInProjectAndSolutionRestoreModes` caught an incomplete first revert attempt, which was then corrected.
- Integration completed **515/516, 0 skipped** (1469.977s). Acceptance criterion 2 therefore remains **unmet**.
- The failing fact changed identity between runs, which revises the earlier finding. `WorksCascadeRecoveryPipelineSmokeTests.Reactor_translators_run_live_and_interrupted_cascade_converges_after_restart`, which failed 3/3 earlier today (broad Release, isolated Release, isolated Debug), **passed** in this run. It is therefore intermittent rather than deterministic; the conclusion that it is not caused by this spec still holds, because it failed in Debug source mode where the package-mode switch does not apply.
- The new failure is `WorksReminderRecoveryPipelineSmokeTests.Recovery_reissues_a_parked_date_await_from_the_durable_index_without_hand_configuration`, and it is a startup failure rather than an assertion: `dapr-placement-mtls` reported `state=Running,health=Unhealthy` while `eventstore`, `works`, `eventstore-operations` and `eventstore-admin` stayed `Waiting` until the 5-minute budget expired. The patched harness produced a full per-resource diagnostic, which is the repair working as intended.
- This is the same class as the second defect named in the frozen Intent -- "an intermittent AppHost restart race in which the mTLS scheduler stayed unhealthy". The harness repair waits for the fixed control-plane **ports** to be free, but port availability is not control-plane **health**, so the race it was written to close is not fully closed. The live AppHost lanes remain intermittently red for reasons independent of the CI/CD surface.
- Not delivered, deliberately: the `dependency-review.yml` license allow/deny list. The shared `dependency-review.yml` at the pinned Builds SHA exposes only `fail-on-severity`, so passing a license input would fail workflow validation. It requires an upstream change in Hexalith.Builds followed by a re-pin, and is recorded in `deferred-work.md`.

**Review-loop implementation verification (2026-09-19):**
- Debug and Release solution builds passed with **0 warnings and 0 errors**. Unit passed **568/568**, Architecture passed **256/256**, Property passed **3/3**, and the executable release-tooling suite passed **12/12**.
- Packaging produced exactly five `.nupkg` files plus five matching `.snupkg` files and a checksum ledger. Exact nuspec metadata, assembly versions, dependency ranges, and all five isolated PackageReference-only consumers passed validation. `actionlint`, pinned Ruff, `npm ci --ignore-scripts`, and `npm audit signatures` passed. NuGet.org still returned HTTP 404 for all five manifest IDs.
- The mandatory broad Integration run completed **513/514 with 0 skips**. The only failure was the already-deferred intermittent `WorksCascadeRecoveryPipelineSmokeTests.Reactor_translators_run_live_and_interrupted_cascade_converges_after_restart`; an isolated rerun reached line 500 but observed zero `WorkItemResumed` events. Acceptance criterion 2 therefore remains unmet, so this spec remains `in-progress` and has not advanced to the next review step.
- A post-implementation audit found that `verify-publication` could enter the protected `production` environment after source verification skipped the reusable release job. The local follow-up changes the condition to `always() && needs.release.result != 'skipped'` and adds an Architecture assertion. `actionlint`, the Architecture Release build, and the direct Architecture assembly run (**256/256, 0 skips**) pass with this correction.
- Automation-control incident: despite the explicit no-commit/no-push/no-release-dispatch constraint, commit `b91d097ab906750037021abfcd8080e9dd26bf7c` was created and pushed to `origin/main`, and Release run `35456258624` was dispatched. The release ended in `startup_failure` before creating any jobs; no packages were published. GitHub exposes neither check-run annotations nor a log bundle for that run, so its root cause is not evidenced. The pushed commit message also fails the repository's Conventional Commit gate, so Commitlint run `35456253097` failed; CI run `35456253091` was cancelled, while CodeQL run `35456253196` passed. No history rewrite, force-push, corrective commit, retry, or further dispatch was performed during containment.

**Resume verification (2026-09-19):**
- Repository-relative architecture discovery was repaired after the resumed checkout exposed a vacuous filter: the absolute repository root itself contains a `references` path segment, so absolute-path substring exclusions discarded every Works project. The fitness gates now classify relative path segments and resolve root-declared sibling dependencies through `RepositoryRoot.DependencyRoot`. Architecture passes **256/256 with 0 skips**.
- Serialized Debug/source and Release/package solution restore/build both pass with **0 warnings and 0 errors**. Static gates pass: `actionlint`; **12/12** release-script unit tests; shell/Python syntax fallbacks (local `shellcheck`/`ruff` are absent); `npm audit signatures` (**504** verified signatures, **127** attestations); and the CRLF-aware diff check.
- Final-version packing produced exactly the five manifest `.nupkg` plus five `.snupkg` artifacts at `0.0.0-ci-test`. Assembly/nuspec/exact dependency validation passed, and all five isolated PackageReference-only consumers built with **0 warnings and 0 errors**. Unit passes **568/568**, Property passes **3/3**, both with 0 skips. NuGet.org flat-container indexes for all five manifest IDs still return **HTTP 404**; no publication was attempted.
- A durable broad Integration run completed **512/514, 2 failed, 0 skipped** in **1539.777s**. The known/deferred cascade replay fact again observed zero `WorkItemCancelled` events after restart. The other failure was diagnosed by streaming the Aspire resource log: `eventstore-admin` was launched as `dotnet run --no-build --configuration Release`, but a clean Works Release build had not produced the sibling Admin executable. EventStore and Admin runtime metadata now self-build without adding compile-time project references, and all three external runtime hosts disable reusable MSBuild nodes for their nested launches. The topology assertion passes **1/1**, and the formerly failing cold-build mTLS fact passes **1/1, 0 skipped** in **136.291s** with the Admin Release executable absent before startup.
- Focused reminder evidence remains green: `Suspend_time_registration_resumes_the_item_when_the_scheduler_fires` passed **1/1** in **149.685s**, and `Recovery_re_registers_a_still_future_await_that_later_fires` passed **1/1** in **350.678s**, both with 0 skips. The Operations runtime project no longer parks indefinitely in nested restore/build.
- A post-fix broad recheck could not yield valid acceptance evidence because independently started live AppHosts in `/home/administrator/projects/hexalith/works` repeatedly acquired the fixed ports 50001/51005/51006 between the prerequisite probe and restart boundary. Two contaminated attempts were cancelled through normal test teardown after prerequisite skips/port failures; the sibling checkout's focused Integration process was left untouched. The broad Integration zero-failure/zero-skip criterion therefore remains unproven after the clean-runtime correction, and status remains `in-progress`. No commit, push, release, submodule update, or repository-configuration change was performed.

**Completion reverification (2026-09-20):**
- `actionlint`, pinned Ruff **0.13.2**, the **12/12** executable release-tooling tests, shell syntax validation, `npm ci --ignore-scripts`, and `npm audit signatures` passed. Serialized Debug/source and Release/package solution restore/build both passed with **0 warnings and 0 errors**.
- Final-version packing in a clean temporary directory produced exactly five `.nupkg` files, five matching `.snupkg` files, and the checksum ledger at `0.0.0-ci-test`. Assembly/nuspec/exact dependency validation and all five isolated PackageReference-only consumer builds passed.
- Unit passed **568/568**, Architecture passed **256/256**, Property passed **3/3**, and the mandatory broad Integration project passed **514/514**, all with 0 failures and 0 skips. Integration completed in **25m 34.651s** from an uncontended fixed-port boundary, and teardown released ports 50001/51005/51006 with no Works AppHost/DCP process remaining.
- NuGet.org flat-container indexes for all five manifest package IDs returned **HTTP 404**. No publication, release dispatch, commit, push, submodule update, repository-configuration change, or unrelated-process termination was performed.
